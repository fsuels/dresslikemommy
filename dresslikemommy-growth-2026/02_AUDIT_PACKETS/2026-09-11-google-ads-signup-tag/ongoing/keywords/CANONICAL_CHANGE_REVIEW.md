# Scoped local correction for root review

**Candidate only; not applied by this lane.** The user's request to correct and continue the Google work covers this reversible local correction. No additional user permission is needed for the five-file local edit after root's independent review. It does not authorize an external change, spend, keyword import or campaign duplication.

Exact proposed targets are `ops/marketing/keyword_strategy.md`, `keyword_factory_015_cpc_criteria.md`, `keyword_scoring_rubric.md`, `us_primary_keyword_lane.md` and `keyword_universe.csv`. No other canonical file is part of the patch; root owns any required command-layer chronology and continuity update separately.

The Markdown changes qualify May live-state descriptions as historical, replace assumed CPA/stop rules with actual retained-revenue economics, require current CPC/serveability evidence and prevent broad negative false positives. R includes retained merchandise plus shipping excluding tax; V is actual variable cost; H is disjoint overhead/other costs. The formula matches the already reviewed playbook and economics lane.

The CSV retains its 18-column schema, 105-row order and all identities, promotion statuses, route/action fields and non-economic scores. Only `economic_fit_10`, `serveability_10`, `total_score` and `threshold` may change. Zero evidence credit does not claim zero demand. Historic source facts remain in the exact baseline snapshots and patch; no archive is rewritten.

`canonical_corrections_manifest.json` contains every target hash and each row's expected before/after fields. `validation.json` records five observed failure cases against baseline and candidate, plus identity/status, watchlist, arithmetic, route, provenance and authority holdouts. This is an auditable candidate, not self-certification for promotion; root's independent reviewer must inspect semantics.

Before applying, require all five target SHA-256 values to equal the manifest baselines and `git apply --check` to pass. Apply only `canonical_corrections.patch`, read back the exact fields and run the relevant canonical continuity checks. Fail closed if a peer changed a target.

Rollback: exact original bytes are stored under `canonical_baseline/ops/marketing/`. If all five current files still match candidate hashes, reverse only this patch or restore only these five snapshots, then verify baseline hashes. If anything changed afterward, do not overwrite it; root must reverse only the still-applicable hunks and reconcile the remainder. No live-system rollback is needed because this candidate performs no external action.
