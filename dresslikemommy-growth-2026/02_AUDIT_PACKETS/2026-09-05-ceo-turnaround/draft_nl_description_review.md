Confidence: H for local prewrite verification; rendered outcome remains unverified.

**PASS — exact unpublished-theme correction.** Reviewer did not author the candidate or perform browser/API checks. Both JSONC files parse after their unchanged leading comment, with no duplicate keys. Among 779 leaves, only `sections.collection_seo.meta_descriptions.dresses` changes. Line 710 replaces exactly U+0027 with U+2019 in `familiefoto's` → `familiefoto’s`. All other bytes, keys, formatting and values are identical. Both values contain 150 Unicode characters; the UTF-8 file grows by two bytes.

The variables contain exactly one file, `locales/nl.json`, whose TEXT body equals the candidate byte-for-byte. The mutation is solely `themeFilesUpsert`, targeting `gid://shopify/OnlineStoreTheme/137782591585`. No MAIN target, publication, product, measurement, permission or spending operation is included. Existing stale paid-control restrictions remain intact.

The operator-supplied fresh 2026-09-06 07:01:42 UTC receipt confirms UNPUBLISHED for the target, MAIN for `133290917985`, and unchanged four-file checksums on each. Draft NL MD5 `8c8aefb0ed28e7025f39c4c86443eaee` matches the local before/rollback bytes. Fresh text bodies are not retained in that receipt; independent binding uses its checksum. Expected candidate MD5: `5d5cfd654711de81c47a8a0d178d5a12`.

**Challenge:** the typographic apostrophe preserves the Dutch plural and removes the ASCII apostrophe implicated by the saved double-escaping diagnosis. This reasonably avoids that specific escaping path; it does not repair shared escaping logic or prove rendered success.

**After-stage gates:** read back the target checksum and preserved scope; verify exact corrected 150-character text in meta, OG, Twitter and parsed CollectionPage descriptions, without literal `&#39;`/`&amp;#39;`. The plan includes NL alias verification but omits its exact URL: record the canonical/alias preview URLs and draft identity in the resulting evidence. No MAIN/public release or spend is approved by this review. Rollback restores only `draft_nl_description_before.json` to the same draft file after an exact-current checksum conflict check.

SHA-256 bindings:

- Candidate: `d2563296a2d94eb78925c0a2fac82a7fb11957d9768f4eb87a33aec3d83424fd`
- Variables payload: `db4ca21eeef48156d0a4376c07f3de9c746a0001a9f3054cd865d524c2627e5b`
- Mutation document: `f537290c09871defd28709cebf82f2b7d43981a4d38f2ff408fad053ceab7716`

Next root action: execute the reviewed one-file draft correction through normal action-time review, then verify the stated sinks and alias.

**Postwrite addendum — 2026-09-06, through 07:06:32 UTC: PASS for independent artifact consistency.** This supersedes the prewrite execution/render status above. Root completed the one-file staging operation; I made no browser/API calls. Candidate, variables and mutation SHA-256s remain exactly as recorded above.

The execution receipt records one successful upsert, no job/errors and seven passing source checks. Independent comparisons confirm unchanged roles, all four scoped MAIN checksums unchanged, only draft NL changed, and the other three scoped draft files preserved. Candidate MD5 `5d5cfd654711de81c47a8a0d178d5a12` and 72,353-byte size match the after-state; returned-body equality is operator-reported.

The operator's rendered receipt reports the exact 150-character U+2019 description in meta, OG, Twitter and parsed CollectionPage outputs on both `/nl/collections/dresses` and `/nl/collections/mother-daughter-matching-dresses`. Each recorded canonical equals its own route. Both match the reviewed text without literal apostrophe entities; draft identity and preview-cookie scope are explicit. The known DA alias heading ambiguity is corrected to null/NOT RUN.

These are unpublished-preview observations, not MAIN/publication, complete cart/checkout, supplier-fit, traffic, sales or profit proof. Only four files per theme were compared here; full-manifest verification belongs to the separate independent lane. No spend authority changes. Next integration step: combine this bounded result with that lane's final manifest evidence while retaining the remaining release gates.
