Confidence: H for the cited platform requirements; DLM deployment and failure cause remain UNKNOWN.

TA-02 documentation review, source check through 2026-09-06 11:18:22 UTC. Four primary sources below. Local comparison input: [pixels/README.md](/Users/fsuels/Projects/dresslikemommy/pixels/README.md), SHA-256 `5d99f21af35ff853db16c0ed2ca9002fc7f501d3e6ed678c8c86b7cf0107272a`. Only that local pixel document was inspected; it is a claim inventory, not deployment proof.

1. **VERIFIED — checkout event and supported API.** `checkout_completed` occurs once per checkout, normally on Thank you. With post-purchase/upsell flows it occurs on the first offer page and does not occur again on Thank you. If its triggering page fails to load, no event is emitted. Shopify provides both app-pixel and custom-pixel `analytics.subscribe` examples, event data/context snapshots, and a third-party `fetch` POST example. This is a browser event, not evidence of complete capture of every paid order. The generic POST example does not certify GA4 endpoint CORS or a particular browser transport. [Shopify checkout_completed](https://shopify.dev/docs/api/web-pixels-api/standard-events/checkout_completed)

2. **VERIFIED — Google support and sandbox limits.** Google recommends Google & YouTube for Shopify Google tagging, including the evolving checkout. Google tags and GTM inside Custom Pixels are explicitly unsupported by Google. Sandbox restrictions can lose signals or affect attribution, consent treatment, cross-domain measurement and diagnostics even while some events arrive. The app supports standard event/tag configuration but does not host GTM; customizations can differ. This warning concerns Google tags/GTM: it does not independently certify or prohibit this repository's direct Measurement Protocol implementation. [Google's Shopify custom-pixel limitations](https://support.google.com/analytics/answer/16000892)

3. **VERIFIED — Measurement Protocol validation, identity and consent.** HTTPS POST returning `2xx` proves request receipt, not valid payload, processing or recorded purchase. The web payload requires `client_id`; `session_id` and realistic `engagement_time_msec` support correct session/engagement and Realtime reporting. Google documents obtaining existing tagging identifiers; inventing another identifier does not prove session continuity. Default `RELAXED` validation can accept invalid fields or ignore excess parameters; Google recommends `ENFORCE_RECOMMENDATIONS` during validation. Strict validation rejects applicable timestamps older than 72 hours. MP consent supports advertising-data and personalization states; when omitted, GA4 uses corresponding online-interaction consent. This is not evidence that an unknown Shopify privacy flag means permission. The API secret is documented as organization-private. No validation/event request was sent. [GA4 MP web reference](https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference?client_type=gtag) (updated 2026-08-26).

4. **VERIFIED — deduplication limits.** GA4 transaction-ID deduplication applies to web streams, not app streams, and the guidance describes transactions from the same user. Each order needs a unique, nonempty, non-PII ID; reusing IDs or sending empty strings can suppress real purchases. This source gives no approximately-24-hour dedup guarantee and does not establish safe reconciliation across independently generated client identities. [GA4 transaction-ID deduplication](https://support.google.com/analytics/answer/12313109?hl=en)

**README challenge / INFERENCE.** Lines 14 and 50–61 do not establish current app failure, its cause, Custom Pixels as the only path, or safe dual sending. Line 24's independently persisted identity makes the same-user premise worth checking. Lines 20–24's exact storage/CSP/CORS assertions and line 40's unknown-consent fallback remain unverified here. Shopify's exact per-pixel consent prerequisites, region settings and replay behavior require their own configuration/API proof; neither receipt nor missing events establishes those states.

**Next action:** root should reconcile the current installed app/custom-pixel configuration and Analytics deployment evidence against these requirements before choosing a repair. Keep event receipt, permission, attempted transport, payload validation, recorded purchase, identity/attribution and deduplication as separate checks. No DLM public request, browser action, account change, template edit or tracking write occurred.
