Confidence: H for documented API; M for storage implications. Documentation checked 2026-09-06; no store query or mutation.

**SUPPORTED CONTENT API; FILE-PRESERVING OVERRIDE NOT ESTABLISHED.** Shopify defines `ONLINE_STORE_THEME` and `ONLINE_STORE_THEME_LOCALE_CONTENT`; the latter represents locale-file content with dynamic translation keys. This supports a theme-content translation interface, rather than collection/article SEO fields. [Resource types](https://shopify.dev/docs/api/admin-graphql/latest/enums/TranslatableResourceType#enums-ONLINE_STORE_THEME_LOCALE_CONTENT)

`translationsRegister` creates/updates translations using resource, locale, key and source digest, requiring `write_translations`. Its published-locale example becomes buyer-visible without a theme-publication step. Conversely, `themeFilesUpsert` explicitly overwrites files and requires `write_themes` plus an exemption. These are distinct API operations/scopes; neither distinction grants permission under this session's restriction. [Translation mutation](https://shopify.dev/docs/api/admin-graphql/latest/mutations/translationsRegister), [file mutation](https://shopify.dev/docs/api/admin-graphql/latest/mutations/themeFilesUpsert)

The documentation does **not** establish a separate override store that leaves MAIN file bytes unchanged. Shopify says `t` returns strings from the active locale file. Its Help Center says theme-content translations are saved into locale files, including through supported translation workflows. The explicitly external storage exception concerns checkout/system messages, not these dress SEO strings. [Locale architecture](https://shopify.dev/docs/storefronts/themes/architecture/locales/storefront-locale-files), [theme translation storage](https://help.shopify.com/en/manual/online-store/themes/customizing-themes/language/translate-theme)

For the DA/NL title/description keys, exact API resource/key availability, persistence behavior and restriction applicability remain unverified. Do not label registration either a proven independent overlay or automatically the identical forbidden API call. Next: obtain platform clarification of this exact MAIN translation operation before considering execution; do not test the boundary with a mutation.

Checks: official web documentation read; skill search failed locally (Node dependency, then fetch). No generated operation, mutation validation, publication or storefront verification ran.
