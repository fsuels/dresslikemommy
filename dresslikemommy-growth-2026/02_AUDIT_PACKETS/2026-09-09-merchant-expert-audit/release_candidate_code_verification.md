Confidence: H. **PASS for combined release revision 1 as an unpublished staging candidate.** No new code defect was found in the merged scope. This independent reviewer did not build the release, change its files, or perform external writes. Only this report was written. Root retains rendered acceptance and publication gates.

The exact frozen artifacts under `release_candidate/` were independently hashed before and after review:

- `candidate_files.json`: `3b32bf05196772f7a1939c95f39c1f89601c54ee8bf5a36f0681e5c72c87cf60` — 46 files, 4,410,277 bytes.
- `file_manifest.json`: `32ab0213629000a46449f72671ca20136689446adacd5a3ed1c80e7a5f840cbe`.
- `candidate.patch`: `2a030eaf2da94ae841b9db08aebf2c0996f6fb27948bdec5c32a2a08ca2b491f`.
- `release_delta_from_revision3.patch`: `e5a20fd00f8dc492b7f441579323e8f2c5825c7cdfa5c92e916e10cbd95efd0b`.

All 525 baseline files independently match the recorded MAIN `133290917985` filename/MD5/size metadata in `theme_staging_before.json`, plus their baseline SHA-256 values. All 527 candidate files match the manifest: 481 original files unchanged, 44 modified originals, two new consent assets. Every payload body matches disk, without duplicates, omissions, or extra changes. Independent in-memory replay of all 156 full-patch hunks reconstructs the exact 46 payload files. Rollback contains the 44 exact originals and two removals conditional on matching candidate hashes. Dirty root files were not imported wholesale.

The cart body exactly matches the reviewed draft `137782591585` source recorded in `merge_proof.json` (SHA-256 `e2fed4a6ae3abbba3b39d481dfee9eb755f623e7856b2d1c2a8747fe6c059cf7`). Its only MAIN delta is the recently-viewed IIFE: local product-route validation, active-language links, escaped content, removal of cached currencyless prices, and re-rendering after cart updates. Quantity/payment logic is unchanged. Four SEO leaves total survive: dress title/description in DA and NL. Five Danish guidance values match the prior source; only two replace different revision-3 wording.

Reversing those two dictionary-only replacements makes the entire active PDP JavaScript byte-identical to independently passed Merchant revision 3. The actual section still loads that asset at `theme/sections/main-product.liquid:933`; nullable explicit variant data remains at `theme/snippets/product-desktop-ux.liquid:182`. Thus the reviewed exact-role/all-options/quantity-one initialization, invalid/generic fallback, unavailable guard, later-choice behavior, presentment prices and entity-safe refresh remain intact. No alternative runtime was substituted.

All 35 locale dictionaries parse, with exactly the allowed shipping key and two consent leaves added relative to revision 3, plus the four SEO changes. No removed leaves or merge collisions were found. The consent source has 39 effective files; its four nonlocale files match the reviewed handoff, including 669-byte CSS v2. Footer assets bind at `theme/sections/footer.liquid:7`. The native preferences API runs only on click, without consent arguments; attempt identity, observer cleanup, timeout and focus handling remain intact (`theme/assets/cookie-preferences.js:25`). No interaction with the cart subscription or PDP price observer, or new observer loop, was found.

The confidence card now uses localized checkout guidance and a localized policy link (`theme/snippets/pdp-purchase-confidence.liquid:373`). The date target is absent; the retained legacy layout date function is null-safe. Its positive-control regression demonstrates that the old target would still acquire a rolling date. Country/currency controls are unchanged.

Schema changes exactly follow `structured_data_merge_proof.json`: two invented-expiry assignments, `priceValidUntil`, unconditional return policy, and the single hidden FAQ render are removed. Offer fields and all GTIN branches remain unchanged; `itemCondition` correctly separates from `shippingDetails` at `theme/snippets/jsonld-seo.liquid:315`. Country, taxonomy and review conditional commas remain structurally valid. Homepage Organization and the unused FAQ snippet are unchanged. Source-preservation assertions do not substitute for emitted Liquid JSON parsing.

Independent checks: release integration **10/10**, consent **28/28**, cart **15/16**, and three JavaScript syntax checks. Revision-3 **18/18** functional coverage is reused through exact byte equivalence, avoiding an unnecessary repeat. Worker Theme Check evidence reports **46/46**, zero diagnostics, using cached Shopify schemas after network lookup failed.

The cart failure at `cart_regression.test.mjs:234` expects `main-cart-title` and `#main-cart-footer-subtotal`, present only in unrelated dirty-root edits. MAIN Liquid has neither ID; its legitimate subtotal target is `.js-contents`. This is a test-contract mismatch, not a new runtime/subtotal failure. MAIN’s heading count lies outside that replacement region (`baseline/sections/main-cart-items.liquid:31`), so its own quantity updates can leave the heading stale; this untouched cosmetic behavior does not block staging. Root’s reported real quantity/subtotal/cleanup and checkout checks are separate evidence.

Next action: root stages only this frozen payload to unpublished `137881223265`, then verifies desktop/mobile variant/manual/generic journeys, currency/cart, DA/NL metadata, shipping/returns, consent/footer, and emitted Product/Offer JSON with and without GTIN. No rolling shipping date, invented expiry/universal returns, or FAQ emission may remain. Managed-dialog English on Arabic remains an acknowledged limitation. Continuation: “Validate the frozen combined preview against these gates before any release decision.”
