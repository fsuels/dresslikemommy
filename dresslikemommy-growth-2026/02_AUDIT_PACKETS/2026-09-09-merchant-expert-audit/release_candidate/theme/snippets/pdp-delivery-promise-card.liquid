{%- comment -%}
  DEPRECATED — superseded by `pdp-purchase-confidence.liquid`.

  The standalone delivery-promise card was consolidated into a unified
  Purchase Confidence module that owns Shipping + Returns + Security in
  one block on 2026-05-11. This file is intentionally rendered empty so
  any lingering `{% render 'pdp-delivery-promise-card' %}` is a no-op
  rather than a Liquid error. Safe to delete from the repo once you've
  confirmed nothing references it.
{%- endcomment -%}
