# Existing feed lifecycle code inventory — 2026-09-10

**VERIFIED (repository only): no production-oriented Google/Merchant lifecycle publisher was found.** The repository has Shopify publication repair tools, static supplemental Google exports, a scheduled validator, and a Pinterest feed generator/host. No inspected code automatically reconciles Shopify additions, updates, archives/deletions, translations, and market changes into Merchant Center. Deployment and native Google-app configuration were not inspected.

| Existing component | Exact evidence | Lifecycle support and limit |
|---|---|---|
| Shopify publication repair | `ops/scripts/shopify_catalog_cleanup.py:407`, `:483`, `:640`, `:795` | Explicit CLI publish/unpublish with post-action verification. ARCHIVED products can be removed from Google and three historical market catalogs; eligible ACTIVE products can be added to Google. DRAFT is skipped at `:443`; zero-market products are skipped at `:446`; deleted Shopify products cannot appear in its scan. Market titles are fixed at `:33`. This is repair logic, not a scheduler or Merchant offer publisher. |
| Single-product republish probe | `ops/scripts/google_publication_republish_probe.py:2` | Incident diagnostic unpublish/republish. Not suitable as an automatic lifecycle engine. |
| Google pilot/supplemental artifacts | `ops/scripts/build_feed_engineering_pilot.py:30`, `:773`, `:838`, `:1006`; `ops/scripts/build_merchant_center_age_group_fix.py:2`, `:351`; `ops/scripts/build_merchant_center_clean_label_upload.py:32` | Pilot filters ACTIVE + Google-published parents, emits historical `shopify_US_{product}_{variant}` IDs and attribute corrections. Other generators produce matched supplemental columns. They do not submit a primary multilingual catalog or delete missing offers. |
| Merchant diagnostics/deletion | `ops/scripts/export_merchant_center_api_diagnostics.py:4`, `:26`, `:274`; `ops/scripts/delete_merchant_supplier_domain_matches_live.py:2`, `:94`, `:280` | Read-only API diagnostics and an incident-specific browser-RPC deletion script. Both contain an old Merchant account target, unlike current 513542500. The deletion tool is not normal lifecycle reconciliation; do not reuse it for this task. |
| Nightly validator | `.github/workflows/feed-validate.yml:4`, `:44`, `:73`, `:89`; `scripts/validateFeed/validateFeed.js:285` | Scheduled Shopify export → validation → GitHub report artifact. No Google upload. Missing credentials fall back to `tmp_products.json` (`workflow:48`), so this workflow cannot certify current synchronization. Run/deployment status is UNKNOWN. |
| Pinterest generator | `ops/scripts/generate_pinterest_feed_grouped.py:144`, `:163`, `:206`, `:349`, `:436`, `:521` | Regeneration includes ACTIVE parents and rewrites a local TSV, so removed parents disappear only from a newly generated file. Product pagination exists; nested variants stop at 100 without pagination. IDs include market/product/variant (`:409`). No translation retrieval, contextual currency pricing, or actual market-membership filter is present. Parent URL ignores market; base Admin price is reused. Do not repurpose wholesale for Google. |
| Pinterest market merger | `ops/scripts/build_pinterest_unified_feed.py:11`, `:47`, `:59`, `:89` | Six hard-coded market labels, all English; adds audit country/language columns. Generates/merges files manually, without upload or stale-offer reconciliation. |
| Pinterest serving infrastructure | `ops/cloudflare/pinterest-feed-worker/src/worker.js:125`, `:136`, `:148`; `wrangler.toml.example:24`; `agent-backend/src/index.js:61`, `:85` | Worker GETs an existing R2 object; Express streams an existing file. Neither refreshes Shopify data, handles product webhooks, publishes Google offers, or reconciles deleted IDs. Current hosting availability was NOT RUN. |

**Reuse decision:** investigate the installed Shopify Google & YouTube integration first. The manual publication audit and validators offer small reusable pieces; the Pinterest host is a separate owner/surface and its row semantics are unsuitable for direct Google reuse. No `publicationUpdate`/`autoPublish` implementation was found in the inspected runtime/scripts.

If native integration leaves verified language gaps, missing custom infrastructure includes supported primary-source ownership, complete country/locale/contextual-price extraction, durable offer identity, safe full-refresh or deletion reconciliation, atomic last-good publication, retries/alerts, and a deployed schedule/webhook. Local static TSV generation alone would not fulfill the user’s automatic-sync request.

**Method/limits:** read-only `rg` inventories and numbered source reads across runtime code, scripts, theme files and hidden deployment/workflow configuration; dependency trees, large evidence exports and history were excluded from broad runtime searches. No credentials, account/network/native/RPC calls, live scripts, tests, source edits, or deployment actions were executed. Only this report was written.
