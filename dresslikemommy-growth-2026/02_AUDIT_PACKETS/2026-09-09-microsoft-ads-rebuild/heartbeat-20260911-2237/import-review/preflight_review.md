# Independent paused-shell import review

**Verdict: GO_FOR_EXACT_PAUSED_SHELL_IMPORT.** The reviewer did not build the file, select native options, or execute account changes. All 22 final checks passed, following 12 exact input checks.

The allowed action is one native Import for exactly two empty Paused Search shells in account 477439/customer 770182, using the frozen file and options. Both native pause controls are on, only new Campaigns is selected, all existing updates and automatic bid/budget raises are off, and the two names are absent from the 11-campaign before-state. This does not authorize ads, targeting, launch, or spending.

The critical challenge is that mapped columns do not prove saved status, bid strategy or cap. The empty, doubly paused scope contains that uncertainty while the native result is tested. Root must recheck the final UI, import once, and then confirm exactly two creations, zero updates/children, all 11 old campaigns unchanged, and each new shell saved with Paused/Search, English or German, 5 or 3 USD daily configuration, and Maximize Clicks with a 0.15 USD maximum CPC. Capture every resulting ID. A partial or unexpected result must not trigger a blind retry.

If any field is rejected or coerced, hold the new shells paused and empty and diagnose the exact result. An unexpected Active new shell may be contained by pausing only that exact newly created ID. Do not alter an old campaign, silently omit the cap, or delete records as a generic rollback.

US/DE names remain labels until geography is configured. Actual CPC, purchase/consent acceptance, contribution economics and paid activation remain unverified and outside this verdict. The source receipt is root's native evidence; this was independent evidence review, not independent browser replay.

See `preflight_review.json` for frozen hashes, all checks and action-time conditions.
