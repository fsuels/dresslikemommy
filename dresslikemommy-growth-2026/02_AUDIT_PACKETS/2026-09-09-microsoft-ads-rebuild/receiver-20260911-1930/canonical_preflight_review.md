# Independent canonical preflight

**PASS — exact ten-file plan.** Ten bounded checks bind plan `097ee49abe7667ef5422eeb39ae53c8ee910ce104d8ae10924bb8ca39ed5d40b`. Reviewer did not build or execute the plan or replay the browser.

All ten private before snapshots and current hashes match. Six replacements each occur once; four entries append. Independent inverse restores every original byte. Other297 coordination table lines,132 queue lines, historical records, parent/X/Merchant changes, full-paid control and parent V7 One Owner Action are preserved. The JSON records reproducible guard selection.

The five native Saves/six fields reconcile to reviewed goal and ad-control receipts. The14-field local landing correction is applied, with16root/7independent checks and current CSV hashes matching. All11 native campaigns remain paused in the dated receipts; this does not mean the new local campaigns exist in the account.

The handoff now correctly separates94 receiver events from the last event four days ago, records purchaseHealthy as no regional sample, and preserves unknown value/dedup/consent/profit. The AU CPC field was only an unsaved inspection. The empty nondeleted scheduled-import table and configured heartbeat do not establish historical imports, all automation disabled or an executed recurring run. No new spend authority is introduced.

Before application, root must refresh exact hashes and stop on drift. Afterward, verify expected after hashes/inverse and run renderer/integration/strict checks before release. This PASS covers the local plan, not purchase or campaign-launch acceptance.

Evidence: [frozen plan](canonical_update_plan.json), [checks, inputs and expected hashes](canonical_preflight_review.json), [current readback](READBACK.md), [landing postflight](landing_revision_postflight_review.json).
