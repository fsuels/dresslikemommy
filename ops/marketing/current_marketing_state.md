# Current Marketing State

Last reconciled: 2026-05-15 11:47 EDT
Source level: repo-known evidence plus 2026-05-14 read-only live reconciliation, public/local paid landing readbacks, repo-local GB/CA/AU keyword strategy repair, fresh GB/CA/AU Ads monitor/gate review, post-sanitizer public final URL and collection-route preflight/reroute, swim-route public readback/reroute, exact 36-row CPC validation packet, 36-row public final URL refresh, 36-row canonical URL packet/readback, GB/CA/AU CPC validation decision kit plus parser fix, authenticated no-pass export attempt, read-only Google Ads API retry harness, Basic Access application/email-watch readback, current P0 blocker readback, existing authenticated Google Ads UI current-serving/search-term readback, remaining dirty collection-route cleanup/exclusion approval packet, US keyword route unblock/reroute packet, US active-product public proof packet, no-upload US Search active-product validation packet, US Standard Shopping query/title diagnosis plus public PDP fit preflight, held-PDP repair/exclusion packet, local seasonal related-product filter, seasonal live-sync approval packet, authenticated export join prep, Standard Shopping item-export join/readback, clicked-PDP public readback, clicked Standard Shopping title/display-title conversion cleanup approval packet, current Google Ads purchase-action and Shopify order-attribution readback, Pinterest 333-row paused draft build-ready packet, current-session Pinterest paused-draft UI attempt stopped on budget validation, current-session Pinterest live-launch attempt proving `$0.15` custom CPC path and stopping on exact product-group scope blocker, current-session Pinterest `$1.00` validation-only follow-up stopped on exact product-group scope blocker, Pinterest exact product-group approval packet, Pinterest exact item-ID import/readback, all-markets Pinterest feed grouping diagnosis and guard readback, Google Shopping multilingual expansion queue, current Merchant Shopping issue-export readback for `US/es` and CA/GB/AU, no-write Merchant `US/es` repair/classification packet, current Merchant all-products/source eligibility export for `US/es` and CA/GB/AU, browser-RPC source/status addendum for `US/es` source `10627981690`, Merchant priority-market capacity fix packet, Merchant capacity execution guard, Shopify Markets region prune preview, Merchant capacity live-execution approval/readback packet, owner-approved Shopify region-prune report plus post-prune Merchant RPC readback showing Merchant rows unchanged, saved-export Merchant post-prune paid-cohort intersection, owner `$0.15` CPC correction, the 105-row local keyword universe/rubric build, automation capability inventory, local Merchant capacity diagnosis, command-layer integration audit, and broad continuity integrity guard.

## State Labels

- `REPO_KNOWN`: historical repo evidence says this was true at the cited time.
- `LIVE_READBACK_REQUIRED`: current live state must be checked before any decision or claim.
- `LIVE_VERIFIED`: current-session live readback proves the state for the named evidence scope.
- `STALE_OR_SUPERSEDED`: historical evidence exists but should not drive current action without reconciliation.

## Executive State

- Goal: grow profitable Dress Like Mommy sales through controlled Google Ads and Pinterest execution, targeting about `650% ROAS`.
- Execution issue being fixed: the repo had durable memory but lacked a compact daily command layer for active campaign decisions.
- Current authority: bounded paid-media authority is `APPROVED_ACTIVE` in `spend_authorization.md`: total cap `$80/day`, new/test campaign cap `$5/day`, green-gated quality-checked proactive actions only. Fresh approval is still required for excluded surfaces. Current session instruction is stricter: no external writes unless current-session approval explicitly allows them.
- Current owner escalation: the owner rejected a two-day wait posture and asked for smart aggressive growth, including Pinterest ad creation and Google Shopping for other languages. This session therefore moved every safe lane that could convert into traffic. Pinterest was attempted under exact launch approval from the refreshed `333` clean scope, max `$5/day`, and hard max `$0.15` CPC. The UI proved a compliant CPC path exists only by using `Catalog sales` with `Pin clicks` optimization, `Custom` bidding, and max CPC `$0.15`; ROAS optimization forces Pinterest Performance+ bidding and disables custom CPC. Launch stopped before publish because the product-group selector exposed only broad catalog groups, not the exact `333` active-clean category groups. A follow-up `$1.00` paused-draft validation-only approval was also attempted/read back in the existing product-group selector and stopped before save because exact groups were unavailable. The owner then approved the exact product-group packet; the first label-filter creation path previewed `0` products. The later upload-capable path imported the exact item-ID CSV and exact filter payloads now read back as `201/103/29`, but Pinterest product detail pages still show `0` selected/products, empty previews, `Promote` disabled, and a 24-hour update notice, so no launch occurred. A multilingual Shopping export queue now covers `US/en`, `US/es`, `CA/en`, `GB/en`, and `AU/en`, and the owner has now clarified the market priority order: USA English and Spanish first, Canada English and French second, GB/England English third, and Europe later. Current-session approval was used to remove only the `52` approved non-priority Shopify `International` regions; the edit succeeded, but immediate Merchant export still shows no Canada English/French or GB English rows, so Shopping remains blocked pending Google/Merchant propagation or Google & YouTube/Merchant publishing control readback. External writes still remain approval gated when they require product-group/catalog/feed/source mutations.
- Pinterest feed-schema blocker: the exact product-count issue is not only a 24-hour import delay. All-markets diagnosis and the 09:45 guard readback confirmed current upstream snapshots still submit variants as standalone products without `item_group_id`: `3` expected FAIL snapshots / `0` ERROR, with duplicate-parent clusters in the Pinterest import CSV and Merchant exports. At 10:07 EDT, automation generated Path B grouped TSVs for all six active market handles (`us`, `canada`, `united-kingdom`, `eu`, `australia`, `international`): each generated feed has `6,969` rows, `326` unique parent groups, `0` missing `item_group_id`, and `0` supplier/source host hits; the guard now reports those `6` Path B snapshots PASS while the `3` upstream snapshots remain expected FAIL. Before any Pinterest launch, the owner must approve the master all-markets channel grouping phrase or a per-market phrase; if the channel UI lacks a grouping toggle, Path B upload/import needs separate exact approval and after-state readback.
- Vendor/brand drift prevention: a prior current-session approved live Shopify backfill set `vendor="Dress Like Mommy"` on `287/287` active products that were not already compliant. This automation repaired the durable local scripts and Flow artifact, then ran the read-only Shopify verifier at 10:18 EDT: `326` active products checked, `0` non-compliant products, verdict `PASS`. Remaining owner-side steps are importing the Shopify Flow workflow and applying Merchant Center feed rules A/B/C; Merchant brand/gender/age/identifier readback waits for rule apply and feed refetch. This does not replace the Pinterest `item_group_id` grouping work.
- Standard Shopping measurement decision: the owner-provided outside diagnosis that purchase tracking is broken/missing or that the active purchase action was removed is not supported by current readback. `Google Shopping App Purchase` remains the primary/included account-default purchase action and last received a purchase request at `2026-05-11T21:47:18Z`. Sanitized Shopify orders since `2026-04-29` show `13` non-cancelled non-test orders, `0` Google paid/CPC signals, and `4` Google organic/product-sync signals. The current fix is not conversion-goal surgery; keep purchase goals unchanged, judge Shopping by primary purchase conversions/value, and move landing/product/feed quality actions only through exact approval gates.
- Self-owned measurement repair lane: GA4 ecommerce reporting is still too weak for paid-growth decisions, so local-only Shopify Custom Pixel artifacts are now install-ready but not live. `pixels/ga4-custom-pixel.js` uses GA4 Measurement Protocol and `pixels/google-ads-custom-pixel.js` uses a native Google Ads conversion beacon; both are consent-hardened for Shopify's current Custom Pixel runtime (`init.customerPrivacy` plus `customerPrivacy` / `api.customerPrivacy` updates). The Ads pixel now captures consented `gclid`/`gbraid`/`wbraid` landing URL parameters in sandbox `browser.localStorage` for 90 days, sends bare numeric order ID as both `oid` and `transaction_id`, and docs now state that Google Ads deduplication is per conversion action, so the native action should validate as Secondary before replacing the GA4-imported Primary. Owner-side GA4 API secret creation, Ads conversion-action creation, Shopify Customer events placeholder replacement/connect, and DebugView/Tag Assistant/test-order verification remain required. The real secret/action values must stay out of the repo and be inserted only in Shopify's editor or a non-repo temporary copy. No live conversion, GA4, Ads, Shopify Admin, order/payment, or Google & YouTube toggle write has occurred.
- Growth urgency: starting 2026-05-14, each day without sales growth, usable learning, or a sales-moving improvement is a failure signal; zero impressions after 24 hours requires same-day serving diagnosis and high-buyer-intent long-tail or auction-entry action planning.
- Proactive action mandate: monitor loops are not the deliverable. If a mistake, broken state, underperforming path, or clear improvement is visible, fix it when local/read-only or approved; if live approval is missing, prepare the smallest exact approval packet and keep another safe sales-moving lane moving.
- Keyword strategy correction: active keyword campaigns cannot rely on copied generic head terms. Every active keyword market needs a market/language intent map, localized vocabulary, landing-fit owner, negative watchlist discipline, and anti-cannibalization owner. GB/CA/AU now have repo-local review-only maps; no live keyword/ad/bid/status write occurred.
- US primary-market correction: US is the biggest market and must be explicit in keyword planning. The current command layer has US Standard Shopping as the live US paid lane, so US keyword intelligence should feed product titles, Shopping query diagnosis, product grouping, Pinterest creative language, and future US Search packets. Use `ops/marketing/us_primary_keyword_lane.md`.
- Hard CPC correction: owner cannot pay more than `$0.15` CPC. Current GB/CA/AU head terms and close variants are not viable expansion ideas because keyword UI first-page estimates around `$0.65-$0.74` are far above the cap. Do not raise bids above `$0.15`; do not upload close-head variants such as `[mummy and me dresses]`, `[mommy and me dresses canada]`, or `[mummy and me dresses australia]`. Future rows must follow `ops/marketing/keyword_factory_015_cpc_criteria.md`: build a large local universe, score it, then promote only validated market-specific long-tail buyer-moment candidates at `$0.15`.
- Keyword universe correction: build big locally, upload small. Current local seed is `105` rows in `ops/marketing/keyword_universe.csv` with US first (`60` rows), GB/CA/AU adapted (`15` each), and `GREEN/YELLOW/RED` thresholds from `ops/marketing/keyword_scoring_rubric.md`. It is not a live upload file.
- Automation capability inventory: shell, repo writes, and network are usable in this runtime. `AUTOMATION_CAPABILITY_MISMATCH` still applies to account-surface work when the needed path is unavailable: Chrome DevTools list-pages is profile-locked, Google Ads API `GOOGLE_ADS_CUSTOMER_ID` is unset in this shell, the earlier controllable Google Ads page redirected to sign-in, and Computer Use must not be probed or repaired during this unattended run. Do not claim Google Ads/Merchant account-surface parity until that path is restored, credentials are loaded in an account-capable shell, or an authenticated export exists.
- P0 blocker cleanup: false account-access blockers and Pinterest controllable access are fixed/readback-passed, not active P0 blockers. The remaining active P0 action gate was attempted through an existing authenticated Google Ads/Keyword Planner session at 16:24, but the export stayed `United States` / `All languages` / `Broad` / `Maximize conversions`, forecast aggregate `Avg. CPC $4.18`, and the patched parser returned `0` `PASS_015_CPC_GATE` rows. The read-only API harness is built and local config is ready, but Google Ads API forecast is blocked by Explorer access until Basic/Standard access is approved. Basic Access was submitted at 03:13 EDT, and Outlook searches of `info@dresslikemommy.com` found no approval as of 06:06 EDT. Existing authenticated Google Ads UI readback on 2026-05-15 showed enabled-campaign aggregate current serving (`6` impressions, `1` click, `$0.04`, `0.00` conversions/value) but not a CPC-gate pass; the visible GB exact Search row remained `0` impressions/clicks/cost. No `GREEN` action row exists; all live keyword/bid/status/negative execution stays blocked until a GB/CA/AU exact/phrase max `$0.15` row-level export or API forecast produces real pass rows.
- Command-layer integration guard: strategy docs that are not wired into action surfaces are now treated as defects. Initial audit found `4` side-document risks; current generated audit covers `25` tracked marketing files and reports `0` risks. Use `ops/scripts/audit_marketing_command_integration.py --write-report --fail-on-risk` before calling new `ops/marketing/` artifacts complete.
- Broad continuity integrity guard: continuity/prompt/cockpit/spend/worklog/handoff changes must pass `python3.13 ops/scripts/check_continuity_integrity.py --strict`. It verifies canonical worklog anchors, quarantined alternate worklogs, latest-anchor resolution in the canonical prompt, spend-authority agreement, cockpit freshness, marketing audit pass, and `AGENTS.md` / `CLAUDE.md` parity.
- Latest local sales-moving unblock: post-sanitizer public readback shows the current GB/CA/AU active Search PDP final URL source is supplier-clean across two header/cache variants. Collection-route preflight found `mommy-and-me`, `family-matching`, and `pajamas` clean; this run rerouted GB/CA/AU matching-dress, vacation, daddy, and swimwear keyword rows away from dirty/broken routes to clean product-relevant routes. `/collections/family-swimsuits` passed GB/CA/AU public readback across two header variants with `200`, `0` supplier/url-brand hits, and family swim copy. `36` GB/CA/AU `GREEN` rows now have clean routes and a local authenticated-validation packet (`GB=12`, `CA=12`, `AU=12`). A 14:57 EDT canonical packet converted `11` redirecting `/collections/family-matching` rows to `/collections/matching-outfits` and rechecked all `12` unique market/route URLs with `24` public fetches: all returned `200`, with `0` redirects, `0` supplier/source-domain or URL-brand hits, and `0` stale seasonal/local-inventory trust hits. The decision kit now provides market-specific Keyword Planner inputs, a `72`-row exact+phrase validation matrix, a forecast export template, the patched `validate_keyword_planner_forecast_export.py`, and the read-only API retry harness `run_google_ads_api_cpc_forecast.py`. At 16:24 EDT an authenticated Keyword Planner attempt exported real Google CSVs, but the UI context was invalid for the gate and parser summaries returned `0` pass rows; the next account-capable path should use the API harness or a UI path with explicit GB/CA/AU, exact/phrase, keyword-level max `$0.15` proof.
- US Shopping and US keyword unblock: because the top P0 Ads validation row remains account-surface gated, the next safe lanes were Standard Shopping query/title diagnosis, US keyword-route cleanup, public active-product proof, no-upload US Search validation packet prep, Standard Shopping item-export joining, clicked-PDP public readback, clicked PDP title/display-title conversion cleanup approval prep, Merchant Shopping issue-export analysis, a no-write Merchant `US/es` repair/classification packet, the current Merchant all-products/source eligibility export, and the priority-market Merchant capacity cleanup packet. The read-only Google Ads products export for campaign `23802638621` joined `767` paid-cohort rows: `112` rows had impressions, `65` clicks, `$14.17` cost, and `$0.00` conversion value; `85` rows matched the public-clean scope, `30` matched held gates, and `0` feed-title approval candidates were found. The clicked-PDP readback then checked `27` clicked export rows across `13` unique handles / `26` public fetches; all `26/26` passed with `0` source-blocked clicked handles. The conversion follow-up checked the `13` unique clicked handles and found `12/13` visible product H1s contain literal ellipses, covering `64/65` clicks and `$13.96/$14.17` cost; all checked pages still had add-to-cart, customer-photo section markup, and hidden zero-review badge behavior. Current Merchant issue export for `US/es` now shows `1,453` issue rows / `354` unique items and `53` paid-cohort issue items, led by over-capacity, Missing age group, Missing color, and Missing gender. The classification packet narrows paid-cohort attribute-repair candidates to `3` unique paid-cohort items, but all `53` paid-cohort issue items remain caught by the Shopping ads over-capacity blocker. Current all-products export parsed `351,007` rows: `US/es` has `5,412` rows / `772` TSV paid-cohort rows and remains blocked; browser RPC addendum confirms all `5,412` `US/es` rows are source `10627981690` and `4,910` are strict-approved by product-list raw status, but that does not override the issue/capacity blocker. The priority-market capacity packet classifies the all-products footprint as USA English `5,491`, USA Spanish `5,412`, Europe-later `134,932`, Asia/Middle East `129,112`, Africa `37,511`, South America `8,818`, non-US-USD `24,243`, and Oceania/NZD `5,488`; first cleanup should remove `199,684` non-priority rows while preserving USA English/Spanish and Europe-later groups. CA/en, CA/fr, and GB/en have `0` rows and no CAD/GBP feed labels/currencies, so they must be enabled/exported only after capacity cleanup. No negative, bid, budget, status, product-group, product, feed, capacity, or theme write is justified from this evidence; Shopify title/display-title cleanup is approval-gated by the new packet. Evidence: `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-standard-shopping-clicked-title-conversion-approval/STANDARD_SHOPPING_CLICKED_TITLE_CONVERSION_APPROVAL_PACKET.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-standard-shopping-clicked-pdp-readback/STANDARD_SHOPPING_CLICKED_PDP_PUBLIC_READBACK.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-shopping-readonly-export-queue/standard_shopping_products_export_summary.json`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-merchant-shopping-readonly-queue-readback/MERCHANT_SHOPPING_READONLY_QUEUE_READBACK.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-merchant-us-es-repair-classification/MERCHANT_US_ES_NO_WRITE_REPAIR_CLASSIFICATION_PACKET.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-merchant-source-all-products-export-attempt/MERCHANT_ALL_PRODUCTS_SOURCE_ELIGIBILITY_READBACK.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-merchant-source-eligibility-browser-rpc-export/MERCHANT_SOURCE_ELIGIBILITY_BROWSER_RPC_EXPORT.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-merchant-priority-market-capacity-fix/MERCHANT_PRIORITY_MARKET_CAPACITY_FIX_PACKET.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-automation-us-search-active-product-validation-packet/US_SEARCH_ACTIVE_PRODUCT_VALIDATION_PACKET.md`, and `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-auth-export-join-prep/US_SHOPPING_AUTH_EXPORT_JOIN_PREP.md`.
- Active-products rule: paid campaigns must advertise only currently active, public, purchasable products with clean landing readbacks. Draft, inactive, stale, excluded, supplier-leaking, seasonally mismatched, or unresolved product URLs cannot enter live traffic.
- Category strategy rule: campaign/category planning must match real shopper intent and the calendar. For example, Father's Day tests should bias toward Daddy-and-Me, father-inclusive family matching products, and broader family matching categories instead of unrelated or stale seasonal pages.

## Repo-Known Paid-Growth State

| Surface | Evidence class | Repo-known state | Current live state | Next action |
|---|---|---|---|---|
| Google Ads GB Search | `LIVE_VERIFIED` | Campaign `23838895360`, exact ad group `Mommy & Me Dresses - Exact`, 3 exact keywords and 1 RSA were previously enabled under exact 2026-05-12 approval | `LIVE_VERIFIED`: 2026-05-14 08:10 monitor passed. Campaign `Enabled` / `Eligible`, `$2.00/day`, Search only, presence-only, no campaign conversion override, only exact ad group `194138528537` enabled, `9` other ad groups paused. Current displayed monitor window still shows `$0.00` cost, `0` clicks, `0` impressions, `0.00` conversions/value. Search-term stale filter was cleared and no search terms are available. Keywords/RSA/final URLs are enabled/country-qualified; keyword UI shows `Eligible (Limited)` with below-first-page-bid estimates around `$0.65-$0.74`, which fails the hard `$0.15` CPC cap. | `LIVE_WRITE_BLOCKED_BY_LANDING_AND_CPC`: no keyword/bid/status action until clean-route candidate long-tail rows are validated at `$0.15`; no bid raise and no close-head-term variants |
| Google Ads CA Search | `LIVE_VERIFIED` | Campaign `23834423669`, same approved exact micro-cohort | `LIVE_VERIFIED`: 2026-05-14 08:10 monitor passed. Campaign `Enabled` / `Eligible`, `$2.00/day`, Search only, presence-only, no campaign conversion override, only exact ad group `196679079575` enabled, `9` other ad groups paused. Current displayed monitor window still shows `$0.00` cost, `0` clicks, `0` impressions, `0.00` conversions/value. Stale `human hair wigs` search-term filter was cleared and no search terms are available. Keywords/RSA/final URLs are enabled/country-qualified; keyword UI shows `Eligible (Limited)` / below first page bid, which fails the hard `$0.15` CPC cap. | `LIVE_WRITE_BLOCKED_BY_LANDING_AND_CPC`: no English or French keyword/bid/status action until clean-route English-Canada long-tail rows are validated at `$0.15`; French-Canada remains separate native-review/landing QA gated |
| Google Ads AU Search | `LIVE_VERIFIED` | Campaign `23834424182`, same approved exact micro-cohort | `LIVE_VERIFIED`: 2026-05-14 08:10 monitor passed. Campaign `Enabled` / `Eligible`, `$2.00/day`, Search only, presence-only, no campaign conversion override, only exact ad group `198852670520` enabled, `9` other ad groups paused. Current displayed monitor window still shows `$0.00` cost, `0` clicks, `0` impressions, `0.00` conversions/value. Stale `human hair wigs` search-term filter was cleared and no search terms are available. Keywords/RSA/final URLs are enabled/country-qualified; keyword UI shows `Eligible (Limited)` / below first page bid, which fails the hard `$0.15` CPC cap. | `LIVE_WRITE_BLOCKED_BY_LANDING_AND_CPC`: no keyword/bid/status action until live sanitizer readback passes and AU long-tail rows are validated at `$0.15`; route beach/swim/holiday terms only to matching clean landings |
| Standard Shopping US | `LIVE_VERIFIED` | Repo memory says live/eligible with tight paid cohort, Standard Shopping constraints, and no permission for status/budget/scope/conversion changes | `LIVE_VERIFIED`: campaign `23802638621` / `DLM_US_STANDARD_SHOPPING_TEST_PAID_READY` is `Enabled` / `Eligible`, Shopping, `$20.00/day`; included `us_test_ready` child groups remain `$0.04`; `Everything else in "All products"` remains `Excluded`. Read-only product export for `Apr 18-May 14, 2026` joined `767` paid-cohort rows with `3,484` impressions, `65` clicks, `$14.17` cost, and `$0.00` conversion value. Public-clean matched rows had `1,744` impressions / `28` clicks / `$5.94`; held rows had only `2` impressions / `0` clicks; unmatched rows had `1,738` impressions / `37` clicks / `$8.23`. Parser found `0` feed-title approval candidates. Clicked-PDP public readback checked `13` clicked handles / `26` fetches and passed `26/26` with `0` source-blocked clicked handles. Follow-up public conversion analysis found `12/13` clicked PDP visible H1s are literally truncated with ellipses, covering `64` clicks / `$13.96` cost. Purchase-attribution readback found `Google Shopping App Purchase` primary/included with last request `2026-05-11T21:47:18Z`, while sanitized Shopify order attribution since `2026-04-29` found `0` Google paid/CPC orders. No negative, bid, budget, status, product-group, feed, capacity, conversion-goal, or theme write is justified; visible-title cleanup requires exact owner approval. | `PURCHASE_ATTRIBUTION_READBACK_DONE__CLICKED_TITLE_CLEANUP_OWNER_APPROVAL_REQUIRED`; keep purchase goals unchanged, do not raise bids from a false broken-tracking diagnosis, use the title/display-title approval packet if the owner approves the exact phrase, and continue Merchant `US/es` issue/capacity plus CA/GB/AU feed availability work before any feed/campaign/product-group action |
| GB/CA/AU optimization | `LIVE_VERIFIED` | Prior evaluator decision was `HOLD_MONITOR_NO_OPTIMIZATION_WRITE` because zero data and search terms were not actionable | `LIVE_VERIFIED`: still no spend/click/conversion data on 2026-05-14; GB search terms readable but empty; CA/AU stale filter remains | Do not add negatives, pause, scale, change bids/budgets, or infer ROAS from stale data; do run same-day zero-impression diagnosis and high-intent long-tail candidate work inside green gates |
| GB/CA/AU active Search landing PDP | `LIVE_VERIFIED_SOURCE_CLEAN` | Final URLs point to the beige chiffon family matching PDP with `?country=GB`, `?country=CA`, and `?country=AU` | Fresh 2026-05-14 11:18 public source readback returned `200` and expected shipping/schema signals across both `Accept: text/html` and `Accept: */*` cache/header variants. Supplier/source-domain hits and URL-like brand attributes are `0` for GB/CA/AU. | PDP sanitizer gate is clear for current final URLs. Next Ads action is still blocked until clean-route `GREEN` rows are validated at max `$0.15` CPC with fresh authenticated Ads readback and reviewer pass |
| GB/CA/AU and US keyword candidate collection routes | `LIVE_VERIFIED_CANONICAL_PACKET_READY__API_HARNESS_READY__US_ACTIVE_PRODUCT_VALIDATION_PACKET_READY__DIRTY_ROUTE_CLEANUP_PACKET_READY` | `keyword_universe.csv` routes the next long-tail candidates to collection pages; local packet now canonicalizes redirecting GB/CA/AU rows, reroutes remaining US dirty/broken route rows to clean alternatives, proves public active-product sample coverage for US `GREEN` rows, and supplies a small US no-upload exact/phrase validation packet. The decision kit plus API harness prepares authenticated forecast classification before CPC validation | Fresh 2026-05-14 12:19 public source readback: `/collections/mommy-and-me`, `/collections/family-matching`, `/collections/pajamas`, and `/collections/family-swimsuits` are supplier-clean for GB/CA/AU with `200` and `0` supplier/url-brand hits. The 14:57 EDT canonical URL packet checked `12` unique market/route URLs with `24` public fetches: `0` redirects, `0` non-200s, `0` supplier/source-domain or URL-brand hits, and `0` stale seasonal/local-inventory trust hits. The decision kit generated Keyword Planner inputs and a patched forecast parser for `72` exact+phrase validation rows. The 15:38 EDT cleanup packet shows `/collections/swimsuits` still has `2` source-vendor products and `/collections/matching-dresses` has `1`, but the 15:59 EDT US keyword route unblock rerouted all `23` US rows from dirty/broken routes to clean alternatives; 04:06 EDT public active-product proof sampled `51` public product pages across `5` clean US routes, with `47` public active-product pass rows and `4` held/review rows. This run built `12` US base rows / `24` exact+phrase forecast rows from public-active clean routes only and public-read back `6/6` route fetches clean. | Validate the canonical 36-row GB/CA/AU packet for `$0.15` CPC/auction entry with `run_google_ads_api_cpc_forecast.py --customer-id 3990976848` after Basic Access approval or through a correctly scoped UI export, then run the decision-kit parser before creating any `GREEN` live action row; for future US Search, validate `us_search_12_active_product_cpc_validation_matrix.csv` only after authenticated CPC/search feasibility is available, and keep original dirty routes excluded until owner-approved cleanup passes readback |
| Google Ads RO/PT/GR/FR/BE builds | `REPO_KNOWN` | RO absent/preview path blocked, PT/GR absent, FR stale/no-change, BE upload-throttle; completed countries must not be re-uploaded | `LIVE_READBACK_REQUIRED` | One-country-at-a-time no-duplicate readback before any preview/apply path |
| ES/IT Golden Daisy Search | `REPO_KNOWN` | Local review-only microtest and native signoff bundle exist; `platform_use_ready=false` pending native review and exact owner approval | `LIVE_READBACK_REQUIRED` before platform use | Native review, validator, then exact approval |
| Pinterest US | `IMPORTED_FILTERS_READBACK_201_103_29_PRODUCTS_ZERO_NO_LAUNCH` | Original local US scope was `342` rows with `4` exclusions. 2026-05-15 public refresh found `2` source-leaking public PDPs inside that scope, so the launch scope is now `333` clean variants and `9` public-source exclusions. Current-session launch approval was given for advertiser `549756244483`, refreshed `333` scope, max `$5/day`, hard max `$0.15` CPC, no catalog/source/tag/CAPI/feed/billing changes, and stop outside scope. The exact product-group packet phrase and exact CSV import path were later approved. | 2026-05-14 15:48 readback: advertiser `549756244483`, account/domain `Dress Like Mommy | Matching Family Outfits` / `dresslikemommy.com`, reporting shows `0 campaigns`, `0 currently being served`, `$0.00` spend, and `0` impressions. 2026-05-15 public scope refresh checked `32` product pages and `32` images: product pages `30/32` pass, images `32/32` pass, refreshed clean scope `333` variants (`family_matching=103`, `mommy_me=201`, `pajamas=29`). Live UI attempt reached `Catalog sales`, daily budget `$5.00`, `Pin clicks`, `Custom` bidding, and max CPC `$0.15`; launch stopped before publish because exact groups were not selectable. The later validation-only follow-up found selected groups `0` and exact groups were still absent. Exact CSV import then created `DLM_PIN_US_SHOPPING_MOMMY_ME_333`, `DLM_PIN_US_SHOPPING_FAMILY_MATCHING_333`, and `DLM_PIN_US_SHOPPING_PAJAMAS_333`; edit readback confirms exact item-ID filter payloads `201/103/29`, but product detail pages still show `0` selected/products, empty previews, `Promote` disabled, and `This product group updates every 24 hours`. No campaign/draft launch occurred. | Freshly read back after Pinterest resolves the import. Continue to final launch review only if usable product counts match exact active-clean scope and review confirms max `$5/day`, max `$0.15` CPC, exact groups, and no source/feed/tag/CAPI/billing/Shopify changes |
| Pinterest feed grouping | `PATH_B_GROUPED_FEEDS_GENERATED__LIVE_UPSTREAM_GUARD_FAIL_EXPECTED` | The Shopify -> Pinterest channel and Merchant mirror evidence use `shopify_<market>_<parent>_<variant>` item IDs with no `item_group_id`, inflating variants as standalone catalog products across every active market/category. The all-markets packet includes a master approval phrase, six per-market approval packets, Path B grouped TSV generator, generated grouped TSVs, and an automated guard wired into continuity. | Automation readback at 2026-05-15 10:07 EDT generated Path B grouped TSVs for `us`, `canada`, `united-kingdom`, `eu`, `australia`, and `international`; each has `6,969` rows, `326` unique parent groups, `0` missing `item_group_id`, and `0` supplier/source host hits. `check_pinterest_feed_grouping.py --report-only --strict` now reports `6` generated feeds PASS and `3` upstream/live-equivalent snapshots still expected FAIL / `0` ERROR. The placeholder freshness marker lacks the required attest phrase, so continuity remains in fix-in-progress mode; no live Shopify/Pinterest/Merchant write occurred. | Owner approves master/per-market channel grouping, or separately approves Path B upload/import from `feeds/pinterest_<market>.tsv`. After channel save or Path B upload and 24h re-sync, capture per-market after-state counts and item_group_id proof, then attest the freshness marker only when strict guard mode can pass |
| Pinterest Event Quality | `REPO_KNOWN` | Repo-known status `Fair`; owner later said assume tags are correct for launch-prep, but writes remain gated. Event quality should not block local/paused prep | `LIVE_READBACK_REQUIRED` | Read-only check if relevant, do not add duplicate tag/CAPI by inference |
| Merchant US/en age_group | `REPO_KNOWN` | Solved in repo evidence; do not redo broad Shopify age_group work | `STALE_OR_SUPERSEDED` for new decisions | Verify only if a fresh diagnostic reopens it |
| Merchant US/es age_group | `ALL_PRODUCTS_EXPORT_CAPTURED__US_ES_BLOCKED` | Source `10627981690`, feed label `US`, language `es`, country `United States`; old exact export showed `625` paid item IDs / `1,250` rows | Current 2026-05-15 Merchant issue export shows `US/es` has `1,453` issue rows / `354` unique items / `53` paid-cohort issue items. Issue mix: `708` over-capacity rows, `432` Missing age group rows, `202` Missing color rows, `86` Missing gender rows, plus page/image/size issues. The no-write classification packet shows only `3` unique paid-cohort items are attribute-repair candidates, but all `53` paid-cohort issue items are still affected by over-capacity. Current all-products TSV parsed `5,412` `US/es` rows / `5,301` in stock / `772` paid-cohort rows; browser RPC source/status addendum confirms all `5,412` rows are source `10627981690` and `4,910` are strict-approved by raw product-list status. | Do not repair from stale May 8 files, sample rows, issue export alone, source row presence alone, or product-list raw status alone. Next valid step is a narrow owner-approved repair/capacity/source decision only after source/approval proof is sufficient |
| Merchant Shopping capacity | `SHOPIFY_REGION_PRUNE_DONE__MERCHANT_ROWS_UNCHANGED__PAID_COHORT_INTERSECTION_DONE` | Not part of the prior command-layer seed | `LIVE_VERIFIED`: Merchant prioritized fixes page updated `3:09 AM May 14, 2026` showed `Over capacity for Shopping ads (outside of CSS program)` affecting `73.3K products (21%)`. Current 2026-05-15 Merchant issue export confirms `US/es` has `708` over-capacity issue rows, including `359` Shopping ads disapproved rows. Current all-products export shows USA English `5,491`, USA Spanish `5,412`, CA/en `0`, CA/fr `0`, GB/en `0`, Europe-later `134,932`, Asia/Middle East `129,112`, Africa `37,511`, South America `8,818`, non-US-USD `24,243`, and Oceania/NZD `5,488`. The local execution guard defines `41` exact preview rows and expected first-pass removal of `199,684` rows. Current-session approved Shopify `International` region prune removed `52` non-priority regions and reduced the market from `73` to `21` regions with protected duplicate `CA` and `AU` preserved, but the fresh post-prune Merchant RPC export still has `351,007` rows, Canada English `0`, Canada French `0`, GB English `0`, and all `199,684` first-pass Merchant removal rows still present. Saved-export intersection proves Standard Shopping export IDs map `767/767` to current US/en Merchant rows, while all `780` exact paid-cohort IDs still appear somewhere in Merchant and `51,033` paid-cohort duplicate rows remain in non-target groups. | Do not repeat a Shopify region-only cleanup and do not call Merchant capacity solved. Next valid unblock is Merchant Center or Google & YouTube publishing control/sync that can preview or remove the exact `feed_label` + `language_code` + `currency` groups in `merchant_capacity_platform_preview_acceptance.csv`, then capture a fresh Merchant all-products export and rerun `build_merchant_capacity_execution_guard.py --after-export`. Canada English/French and GB English Shopping remain blocked until fresh rows exist and the guard passes. |
| Non-US purchase proof | `STALE_OR_SUPERSEDED` | Order-level non-US purchase event currency/value proof remained unclosed in repo evidence; owner later said stop using tags/Event Quality/GA4 proof as launch-prep blocker, but do not overclaim measurement | `LIVE_READBACK_REQUIRED` for reporting claims | Reconcile with owner directive and fresh reporting evidence |
| Beach/Vacation URL | `REPO_KNOWN` | Stale Christmas SEO/social metadata on paid-candidate beach URL is locally mitigated by held Ads CSV excluding that handle; Shopify repair requires exact approval | `LIVE_READBACK_REQUIRED` before traffic | Keep URL excluded or request exact Shopify metadata repair approval |

## Live Readback Status

2026-05-14 read-only reconciliation confirmed, without writes:

- GB/CA/AU exact Search are enabled/eligible at the approved exact micro-cohort scope, with zero spend/click/impression/conversion data for yesterday.
- GB/CA/AU zero-impression action planning is locally complete and the fresh read-only monitor is complete: stale reporting filters were cleared, current keywords are enabled but `Eligible (Limited)` below first page bid, and a corrected blocked packet now rejects head/near-head terms and allows only long-tail validation candidates. No live keyword/ad/bid/status write occurred; active PDP landing sanitizer now passes public source readback, but candidate CPC validation is still required.
- A 105-row local keyword universe is now built and scored: `77` `GREEN`, `20` `YELLOW`, and `8` `RED` rows across US, GB, CA, and AU. Live promotion still requires route-level landing-clean proof, `$0.15` CPC validation, exact action-queue scope, reviewer pass, and after-state readback. After the 12:19 swim-route unblock, `36` GB/CA/AU `GREEN` rows are on clean routes; the 14:57 canonical CSV is the current validation artifact with `0` redirecting final URLs and `0` public source/stale blockers before the account-gated CPC step. The CPC decision kit now supplies Keyword Planner input files, `gb_ca_au_36_keyword_planner_validation_matrix.csv`, `keyword_planner_forecast_export_template.csv`, a patched parser for future authenticated forecast exports, and the read-only API harness `run_google_ads_api_cpc_forecast.py`.
- US Standard Shopping query/title diagnosis is now joined to authenticated product evidence: `41` US `GREEN` keyword rows and `29` US Shopping-ready rows were compared against visible Shopping query evidence and the `780`-variant paid cohort, then the read-only product export for campaign `23802638621` joined `767` rows for `Apr 18-May 14, 2026`. Result: `65` clicks, `$14.17` cost, `$0.00` conversion value, `85` public-clean matches, `30` held matches, `652` unmatched rows, and `0` title/feed approval candidates. Clicked-PDP public readback then checked `27` clicked export rows across `13` unique handles / `26` fetches; all fetches passed and `0` clicked handles were source-blocked. Current purchase-action/order-attribution readback shows purchase tracking is not missing: `Google Shopping App Purchase` is primary/included and received a request on `2026-05-11T21:47:18Z`; Shopify order attribution since `2026-04-29` shows `0` Google paid/CPC orders. No title/feed/product-group/bid/budget/status/negative/conversion-goal write is justified from this evidence.
- Google Shopping multilingual expansion is now active read-only work, not waiting behind Basic Access: `US/en` existing Standard Shopping item export is complete; current Merchant issue-export analysis, no-write `US/es` classification, and all-products/source export are complete. `US/es` has rows but remains issue/capacity blocked; CA/en, GB/en, and AU/en have no rows in the current all-products export. Non-English Shopping beyond `US/es` remains concept-only until native landing/title and current Merchant feed proof exists.
- Standard Shopping is enabled/eligible. Product export for `Apr 18-May 14, 2026` shows `65` clicked product rows worth `$14.17` cost and `$0.00` conversion value in the joined evidence; the current decision is no Shopping write, with Merchant/feed eligibility and conversion/landing analysis next.
- 2026-05-15 authenticated Google Ads UI readback shows the enabled-campaign aggregate current view is no longer strictly zero-serving (`6` impressions, `1` click, `$0.04`, `0.00` conversions/value), but the visible GB exact Search row remained `0` impressions/clicks/cost and the unfiltered broader search-term page showed historical brand Search clicks, not validated GB/CA/AU long-tail or Shopping optimization evidence.
- Google Ads is authenticated in the existing Chrome remote-debugging session for `399-097-6848 dresslikemommy.com`, and Keyword Planner accepted the validation input, but the export context did not satisfy the GB/CA/AU exact/phrase `$0.15` gate. Pinterest Ads Manager is authenticated and controllable in the existing `549756244483` reporting tab.
- Merchant US/es should now be treated as current-issue-export blocked, not sample-cleared: the 2026-05-15 issue export has `432` Missing age group rows and `53` paid-cohort issue items for `US/es`.
- Merchant Shopping capacity local diagnosis is current for `US/es`: the 2026-05-15 issue export has `708` over-capacity rows for `US/es`, including `359` Shopping ads disapproved rows. Current all-products export shows CA/en, CA/fr, and GB/en have `0` rows and no expected CAD/GBP feed-label proof, so they are not Shopping-build-ready yet. The priority-market capacity packet and execution guard identify `41` exact first-pass preview rows / `199,684` rows to remove from Merchant/Google publishing scope. The current-session approved Shopify Markets `International` region cleanup executed and reduced the region list from `73` to `21`, but a fresh Merchant RPC export immediately afterward still had `351,007` rows, all `199,684` first-pass removal rows present, and target Canada/GB rows still `0`; saved-export intersection proves current Standard Shopping rows reconcile to US/en but the full paid cohort is still duplicated across non-target markets. The next control surface must be Merchant/Google publishing sync or exact feed-group control, not another Shopify region-only save and not Shopping expansion.
- ROAS decision state from the saved readback does not justify scale, pause, negative, or bid/budget live action yet; bounded authority is active, and GB/CA/AU zero-impression state now requires same-day diagnosis and high-intent long-tail/auction-entry planning before any green-gated action.
- GB/CA/AU active Search landing public source has correct country/currency/shipping signals and now has `0` supplier/source-domain hits or URL-like brand attributes across two public header/cache variants. Candidate collection route work is unblocked for the current GB/CA/AU validation set: matching-dress, vacation, daddy, and swimwear rows have been rerouted to clean `mommy-and-me`, `matching-outfits`, or `family-swimsuits` routes in the canonical validation packet.

Still needed:

- Source/approval-status proof for `US/es` source `10627981690` if a repair/capacity packet is pursued; the current all-products TSV proves row presence but lacks `source_id` and approved/disapproved destination status.
- Authenticated Merchant/Google publishing control readback for the current capacity blocker. Shopify Markets region cleanup is reported complete, but Merchant row generation did not change in the post-prune export. Use Merchant Center or Google & YouTube controls to reconcile the exact `merchant_capacity_platform_preview_acceptance.csv` feed-label/language/currency rows, trigger only the approved publishing-scope sync/control if available, then capture a fresh all-products export and rerun the after-export guard.
- Pinterest launch/readback from the restored authenticated Ads Manager tab is now blocked by exact product-group resolution, not by CPC or the earlier minimum-budget validation issue. The exact approval phrase and exact upload path were received; the exact CSV imported and filter payloads read back `201/103/29`, but Pinterest still shows `0` selected/products, empty previews, disabled `Promote`, and a 24-hour update notice. Use the refreshed `333`-variant public-clean scope unless the `9` newly held supplier-leaking variants are repaired and read back clean. The smart split is category-based only when exact: Mommy & Me `201`, Family Matching `103`, Pajamas `29`, and Daddy & Me/father-inclusive only after active clean proof.
- Pinterest exact product-group readback: recheck the imported groups after Pinterest resolves the update, require usable exact product counts for `201/103/29`, then run final launch review. Broad Pinterest collection groups are not acceptable substitutes.
- Pinterest feed grouping after-state: do not treat exact product groups as launch-ready until the underlying feed has `item_group_id` grouping across every active market/category, or a Path B grouped feed is approved and uploaded. The current guard failures are expected evidence of the live blocker, not a pass.
- Re-run the exact canonical 36-row clean-route GB/CA/AU packet from `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-36-row-cpc-canonical-url-packet/gb_ca_au_36_clean_route_cpc_validation_rows_canonical_urls.csv` against Keyword Planner/keyword UI or the new read-only Google Ads API harness at max `$0.15`, current active products, and anti-cannibalization ownership before any live Search packet, but only after Basic Access approval or an equivalent correctly scoped account-capable export exists. The 16:24 authenticated UI attempt did not validate because it exported US/Broad/Maximize-conversions aggregate data and parser summaries returned `0` pass rows. On 2026-05-15 the corrected developer token proved valid and both manager/client customers are visible, but Google Ads API blocks both forecast and account-link mutation with `DEVELOPER_TOKEN_NOT_APPROVED` / Explorer access only; the manager UI is still on final business-info confirmation. Use `AUTHENTICATED_GB_CA_AU_CPC_VALIDATION_ATTEMPT.md`, `GOOGLE_ADS_API_CPC_FORECAST_RETRY_HARNESS.md`, `google_ads_api_explorer_access_block_summary.json`, and the decision kit to retry after Basic/Standard API access or after manager UI setup/linking. For US, the no-upload `us_search_12_active_product_cpc_validation_matrix.csv` is ready for future read-only validation from public-active clean routes only; it still needs authenticated `$0.15` CPC/search feasibility and a fresh green row before live use. Keep `/collections/swimsuits` plus `/collections/matching-dresses` rows local-only until `COLLECTION_SOURCE_CLEANUP_APPROVAL_PACKET.md` is approved/executed/read back clean or the rows are rerouted.
- Next GB/CA/AU monitor and serving diagnosis now: check sales/ROAS, serving attribution, eligibility, Quality Score gaps, auction entry/CPC cap, exact keyword competitiveness, saved long-tail alternatives, and stale search-term filters before any live action. Any candidate must prove it can enter at `$0.15` or stay blocked; the 2026-05-15 aggregate `1` click / `$0.04` signal does not satisfy the GB/CA/AU CPC gate.
- Exact owner decision on the clicked Standard Shopping title/display-title cleanup packet. If approved, clean only listed clicked PDP visible titles/display titles and verify before/after H1/title/add-to-cart/price/source-clean/zero-review state. If not approved, keep observing and continue Merchant/feed eligibility. Do not change Shopping conversion goals from the outside diagnosis; if the owner wants a stronger measurement proof, prepare a separate controlled paid-test-purchase approval packet. For future US Search, start only from the public-active rows in `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-active-product-proof/us_active_product_sample_rows.csv`, then validate `$0.15` CPC/search feasibility before any live row.
- Google Shopping multilingual next unblock: `US/es` needs source/approval-status proof and a narrow owner-approved repair/capacity/source packet if pursued; CA/GB/AU need feed/source availability proof because the post-prune all-products export still has `0` CA/en, `0` CA/fr, and `0` GB/en rows. `US/en` existing Standard Shopping item export and clicked-PDP public readback are complete. Do not create campaigns, mutate feed/title/product groups, or change budgets/bids/status from stale exports or concept copy.
- Keep `/collections/swimsuits` and `/collections/matching-dresses` excluded from live traffic until repaired/rerouted, because the public cleanup packet found raw Shopify product JSON supplier vendor leaks on `2` swim products and `1` matching-dress product. The current GB/CA/AU swimwear rows have been rerouted to the clean `/collections/family-swimsuits` route for CPC validation only.
- Full current Admin/Merchant/Pinterest active-product intersection before advertising all active products; the first prep map is saved, but upload/apply decisions still require exact approval and clean readbacks.

## Do Not Infer

- Do not infer live eligibility, spend, ROAS, or blockers from this file.
- Do not infer approval for out-of-bounds actions from bounded spend authority.
- Do not treat local review-only files as platform-ready.
- Do not create a second command layer under `AI_Team/`.
