# Current Marketing State

Last reconciled: 2026-05-14 13:58 EDT
Source level: repo-known evidence plus 2026-05-14 read-only live reconciliation, public/local paid landing readbacks, repo-local GB/CA/AU keyword strategy repair, fresh GB/CA/AU Ads monitor/gate review, post-sanitizer public final URL and collection-route preflight/reroute, swim-route public readback/reroute, exact 36-row CPC validation packet, US Standard Shopping query/title diagnosis plus public PDP fit preflight, held-PDP repair/exclusion packet, local seasonal related-product filter, and authenticated export join prep, owner `$0.15` CPC correction, the 105-row local keyword universe/rubric build, automation capability inventory, local Merchant capacity diagnosis, command-layer integration audit, and broad continuity integrity guard.

## State Labels

- `REPO_KNOWN`: historical repo evidence says this was true at the cited time.
- `LIVE_READBACK_REQUIRED`: current live state must be checked before any decision or claim.
- `LIVE_VERIFIED`: current-session live readback proves the state for the named evidence scope.
- `STALE_OR_SUPERSEDED`: historical evidence exists but should not drive current action without reconciliation.

## Executive State

- Goal: grow profitable Dress Like Mommy sales through controlled Google Ads and Pinterest execution, targeting about `650% ROAS`.
- Execution issue being fixed: the repo had durable memory but lacked a compact daily command layer for active campaign decisions.
- Current authority: bounded paid-media authority is `APPROVED_ACTIVE` in `spend_authorization.md`: total cap `$80/day`, new/test campaign cap `$5/day`, green-gated quality-checked proactive actions only. Fresh approval is still required for excluded surfaces.
- Growth urgency: starting 2026-05-14, each day without sales growth, usable learning, or a sales-moving improvement is a failure signal; zero impressions after 24 hours requires same-day serving diagnosis and high-buyer-intent long-tail or auction-entry action planning.
- Proactive action mandate: monitor loops are not the deliverable. If a mistake, broken state, underperforming path, or clear improvement is visible, fix it when local/read-only or approved; if live approval is missing, prepare the smallest exact approval packet and keep another safe sales-moving lane moving.
- Keyword strategy correction: active keyword campaigns cannot rely on copied generic head terms. Every active keyword market needs a market/language intent map, localized vocabulary, landing-fit owner, negative watchlist discipline, and anti-cannibalization owner. GB/CA/AU now have repo-local review-only maps; no live keyword/ad/bid/status write occurred.
- US primary-market correction: US is the biggest market and must be explicit in keyword planning. The current command layer has US Standard Shopping as the live US paid lane, so US keyword intelligence should feed product titles, Shopping query diagnosis, product grouping, Pinterest creative language, and future US Search packets. Use `ops/marketing/us_primary_keyword_lane.md`.
- Hard CPC correction: owner cannot pay more than `$0.15` CPC. Current GB/CA/AU head terms and close variants are not viable expansion ideas because keyword UI first-page estimates around `$0.65-$0.74` are far above the cap. Do not raise bids above `$0.15`; do not upload close-head variants such as `[mummy and me dresses]`, `[mommy and me dresses canada]`, or `[mummy and me dresses australia]`. Future rows must follow `ops/marketing/keyword_factory_015_cpc_criteria.md`: build a large local universe, score it, then promote only validated market-specific long-tail buyer-moment candidates at `$0.15`.
- Keyword universe correction: build big locally, upload small. Current local seed is `105` rows in `ops/marketing/keyword_universe.csv` with US first (`60` rows), GB/CA/AU adapted (`15` each), and `GREEN/YELLOW/RED` thresholds from `ops/marketing/keyword_scoring_rubric.md`. It is not a live upload file.
- Automation capability inventory: shell, repo writes, network, Playwright MCP, GitHub MCP, and OpenAI docs MCP are usable in this runtime. `AUTOMATION_CAPABILITY_MISMATCH`: authenticated Chrome/account-surface readbacks are not equivalent to a normal owner session here because Chrome DevTools MCP is profile-locked, Computer Use app listing works but interactive access is denied by missing permissions, and shell-side Playwright is not installed. Do not claim account-surface parity for Merchant or Pinterest until that path is restored.
- Command-layer integration guard: strategy docs that are not wired into action surfaces are now treated as defects. Initial audit found `4` side-document risks; current generated audit covers `25` tracked marketing files and reports `0` risks. Use `ops/scripts/audit_marketing_command_integration.py --write-report --fail-on-risk` before calling new `ops/marketing/` artifacts complete.
- Broad continuity integrity guard: continuity/prompt/cockpit/spend/worklog/handoff changes must pass `python3.13 ops/scripts/check_continuity_integrity.py --strict`. It verifies canonical worklog anchors, quarantined alternate worklogs, latest-anchor resolution in the canonical prompt, spend-authority agreement, cockpit freshness, marketing audit pass, and `AGENTS.md` / `CLAUDE.md` parity.
- Latest local sales-moving unblock: post-sanitizer public readback shows the current GB/CA/AU active Search PDP final URL source is supplier-clean across two header/cache variants. Collection-route preflight found `mommy-and-me`, `family-matching`, and `pajamas` clean; this run rerouted GB/CA/AU matching-dress, vacation, daddy, and swimwear keyword rows away from dirty/broken routes to clean product-relevant routes. `/collections/family-swimsuits` passed GB/CA/AU public readback across two header variants with `200`, `0` supplier/url-brand hits, and family swim copy. `36` GB/CA/AU `GREEN` rows now have clean routes and a local authenticated-validation packet (`GB=12`, `CA=12`, `AU=12`) at `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-swim-route-unblock/GB_CA_AU_SWIM_ROUTE_UNBLOCK_AND_36_ROW_CPC_PACKET.md`. The actual `$0.15` CPC validation still needs authenticated Google Ads/Keyword Planner because this runtime has no Google Ads API env keys/package and account-surface GUI access is gated.
- US Shopping unblock: because the top P0 Ads validation row remains account-surface gated, the next safe lane was Standard Shopping query/title diagnosis. Yesterday's Standard Shopping readback had `17` impressions, `0` clicks, `$0.00` cost, and visible zero-click search terms `family pictures outfits`, `family same outfit`, and `mommy and me wedding guest dresses`. The local packet maps those terms to paid-cohort candidate handles and proves no negative/product-group/bid/status action is justified from zero-click zero-cost data. Public PDP fit preflight then checked `24` candidate rows / `10` unique handles: `18/24` rows are clean enough to carry into authenticated item export, `5` rows are held for public source/stale-copy issues, and `1` row needs title-fit review. The held-PDP repair packet rechecked the `6` held/review rows across `3` handles: `3` rows stay excluded until supplier/source-clean, `2` rows were stale seasonal-copy blocked, and `1` source-clean weak-fit row may enter export only if item-level impressions prove relevance. Fresh context readback proved the swim-trunks seasonal hits came from related-product cards, so a local theme filter now skips Christmas/Santa/Xmas recommendations on non-seasonal PDPs. Those `2` rows remain excluded until approved live theme sync plus public source readback shows `0` stale seasonal hits. The automation then built the local authenticated export join prep so the future read-only item export can be joined immediately to the `18` public-clean rows and classify held/unmatched rows safely. Next step is still the authenticated read-only item-level export, now using `run_us_shopping_auth_export_join_prep.py --export-csv`, before any title/feed approval packet. Evidence: `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-query-title-diagnosis/US_STANDARD_SHOPPING_QUERY_TITLE_DIAGNOSIS.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-public-pdp-fit-preflight/US_SHOPPING_PUBLIC_PDP_FIT_PREFLIGHT.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-held-pdp-repair-packet/US_SHOPPING_HELD_PDP_REPAIR_PACKET.md`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-seasonal-related-filter/US_SHOPPING_SEASONAL_RELATED_FILTER_LOCAL_FIX.md`, and `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-auth-export-join-prep/US_SHOPPING_AUTH_EXPORT_JOIN_PREP.md`.
- Active-products rule: paid campaigns must advertise only currently active, public, purchasable products with clean landing readbacks. Draft, inactive, stale, excluded, supplier-leaking, seasonally mismatched, or unresolved product URLs cannot enter live traffic.
- Category strategy rule: campaign/category planning must match real shopper intent and the calendar. For example, Father's Day tests should bias toward Daddy-and-Me, father-inclusive family matching products, and broader family matching categories instead of unrelated or stale seasonal pages.

## Repo-Known Paid-Growth State

| Surface | Evidence class | Repo-known state | Current live state | Next action |
|---|---|---|---|---|
| Google Ads GB Search | `LIVE_VERIFIED` | Campaign `23838895360`, exact ad group `Mommy & Me Dresses - Exact`, 3 exact keywords and 1 RSA were previously enabled under exact 2026-05-12 approval | `LIVE_VERIFIED`: 2026-05-14 08:10 monitor passed. Campaign `Enabled` / `Eligible`, `$2.00/day`, Search only, presence-only, no campaign conversion override, only exact ad group `194138528537` enabled, `9` other ad groups paused. Current displayed monitor window still shows `$0.00` cost, `0` clicks, `0` impressions, `0.00` conversions/value. Search-term stale filter was cleared and no search terms are available. Keywords/RSA/final URLs are enabled/country-qualified; keyword UI shows `Eligible (Limited)` with below-first-page-bid estimates around `$0.65-$0.74`, which fails the hard `$0.15` CPC cap. | `LIVE_WRITE_BLOCKED_BY_LANDING_AND_CPC`: no keyword/bid/status action until clean-route candidate long-tail rows are validated at `$0.15`; no bid raise and no close-head-term variants |
| Google Ads CA Search | `LIVE_VERIFIED` | Campaign `23834423669`, same approved exact micro-cohort | `LIVE_VERIFIED`: 2026-05-14 08:10 monitor passed. Campaign `Enabled` / `Eligible`, `$2.00/day`, Search only, presence-only, no campaign conversion override, only exact ad group `196679079575` enabled, `9` other ad groups paused. Current displayed monitor window still shows `$0.00` cost, `0` clicks, `0` impressions, `0.00` conversions/value. Stale `human hair wigs` search-term filter was cleared and no search terms are available. Keywords/RSA/final URLs are enabled/country-qualified; keyword UI shows `Eligible (Limited)` / below first page bid, which fails the hard `$0.15` CPC cap. | `LIVE_WRITE_BLOCKED_BY_LANDING_AND_CPC`: no English or French keyword/bid/status action until clean-route English-Canada long-tail rows are validated at `$0.15`; French-Canada remains separate native-review/landing QA gated |
| Google Ads AU Search | `LIVE_VERIFIED` | Campaign `23834424182`, same approved exact micro-cohort | `LIVE_VERIFIED`: 2026-05-14 08:10 monitor passed. Campaign `Enabled` / `Eligible`, `$2.00/day`, Search only, presence-only, no campaign conversion override, only exact ad group `198852670520` enabled, `9` other ad groups paused. Current displayed monitor window still shows `$0.00` cost, `0` clicks, `0` impressions, `0.00` conversions/value. Stale `human hair wigs` search-term filter was cleared and no search terms are available. Keywords/RSA/final URLs are enabled/country-qualified; keyword UI shows `Eligible (Limited)` / below first page bid, which fails the hard `$0.15` CPC cap. | `LIVE_WRITE_BLOCKED_BY_LANDING_AND_CPC`: no keyword/bid/status action until live sanitizer readback passes and AU long-tail rows are validated at `$0.15`; route beach/swim/holiday terms only to matching clean landings |
| Standard Shopping US | `LIVE_VERIFIED` | Repo memory says live/eligible with tight paid cohort, Standard Shopping constraints, and no permission for status/budget/scope/conversion changes | `LIVE_VERIFIED`: campaign `23802638621` / `DLM_US_STANDARD_SHOPPING_TEST_PAID_READY` is `Enabled` / `Eligible`, Shopping, `$20.00/day`; yesterday `2026-05-13` shows `17` impressions, `0` clicks, `$0.00` cost, `0.00` conversions/value; included `us_test_ready` child groups remain `$0.04`; `Everything else in "All products"` remains `Excluded`. Visible yesterday terms were `family pictures outfits`, `family same outfit`, and `mommy and me wedding guest dresses`, all zero-click/zero-cost. Local query/title diagnosis plus public PDP preflight now narrows the export scope to `18` public-clean rows, while the held-PDP packet keeps `5` source/stale rows excluded and allows the `1` weak-fit source-clean row only behind item-level impression proof. The export join prep has a template, handle-level scope, and script ready for the future authenticated CSV. No negative, bid, budget, status, product-group, product, feed, or title write is justified yet. | `US_SHOPPING_EXPORT_JOIN_PREP_READY__READ_ONLY_EXPORT_NEXT`; run authenticated read-only product-item export and run `run_us_shopping_auth_export_join_prep.py --export-csv /path/to/authenticated-export.csv` before any title/feed approval packet; keep held-PDP repair rows out of decisions unless repaired/read back or proven relevant |
| GB/CA/AU optimization | `LIVE_VERIFIED` | Prior evaluator decision was `HOLD_MONITOR_NO_OPTIMIZATION_WRITE` because zero data and search terms were not actionable | `LIVE_VERIFIED`: still no spend/click/conversion data on 2026-05-14; GB search terms readable but empty; CA/AU stale filter remains | Do not add negatives, pause, scale, change bids/budgets, or infer ROAS from stale data; do run same-day zero-impression diagnosis and high-intent long-tail candidate work inside green gates |
| GB/CA/AU active Search landing PDP | `LIVE_VERIFIED_SOURCE_CLEAN` | Final URLs point to the beige chiffon family matching PDP with `?country=GB`, `?country=CA`, and `?country=AU` | Fresh 2026-05-14 11:18 public source readback returned `200` and expected shipping/schema signals across both `Accept: text/html` and `Accept: */*` cache/header variants. Supplier/source-domain hits and URL-like brand attributes are `0` for GB/CA/AU. | PDP sanitizer gate is clear for current final URLs. Next Ads action is still blocked until clean-route `GREEN` rows are validated at max `$0.15` CPC with fresh authenticated Ads readback and reviewer pass |
| GB/CA/AU keyword candidate collection routes | `LIVE_VERIFIED_SWIM_REROUTE_DONE` | `keyword_universe.csv` routes the next long-tail candidates to collection pages; local file now reroutes GB/CA/AU rows away from dirty/broken routes | Fresh 2026-05-14 12:19 public source readback: `/collections/mommy-and-me`, `/collections/family-matching`, `/collections/pajamas`, and `/collections/family-swimsuits` are supplier-clean for GB/CA/AU with `200` and `0` supplier/url-brand hits. Matching-dress, vacation, daddy, and swimwear rows are rerouted to clean product-relevant routes. The exact authenticated CPC validation packet now selects `36` rows (`GB=12`, `CA=12`, `AU=12`). `/collections/swimsuits` still leaks raw Shopify product JSON vendor URLs, but no GB/CA/AU validation row now routes there. | Validate the exact 36-row packet for `$0.15` CPC/auction entry in authenticated Google Ads/Keyword Planner |
| Google Ads RO/PT/GR/FR/BE builds | `REPO_KNOWN` | RO absent/preview path blocked, PT/GR absent, FR stale/no-change, BE upload-throttle; completed countries must not be re-uploaded | `LIVE_READBACK_REQUIRED` | One-country-at-a-time no-duplicate readback before any preview/apply path |
| ES/IT Golden Daisy Search | `REPO_KNOWN` | Local review-only microtest and native signoff bundle exist; `platform_use_ready=false` pending native review and exact owner approval | `LIVE_READBACK_REQUIRED` before platform use | Native review, validator, then exact approval |
| Pinterest US | `LIVE_VERIFIED` | Clean local US scope: `342` rows, `4` exclusions, validated paused-draft spec. Owner had approved a paused build, but authenticated controllable Pinterest access was blocked. No Pinterest write occurred | `LIVE_VERIFIED`: 2026-05-14 Pinterest Ads URL lands on public login/sign-up page, Create control not found, login blocker true, and no object saved/created | Restore authenticated controllable Ads Manager access before any paused draft write |
| Pinterest Event Quality | `REPO_KNOWN` | Repo-known status `Fair`; owner later said assume tags are correct for launch-prep, but writes remain gated. Event quality should not block local/paused prep | `LIVE_READBACK_REQUIRED` | Read-only check if relevant, do not add duplicate tag/CAPI by inference |
| Merchant US/en age_group | `REPO_KNOWN` | Solved in repo evidence; do not redo broad Shopify age_group work | `STALE_OR_SUPERSEDED` for new decisions | Verify only if a fresh diagnostic reopens it |
| Merchant US/es age_group | `LIVE_VERIFIED` / `STALE_OR_SUPERSEDED` | Source `10627981690`, feed label `US`, language `es`, country `United States`; old exact export showed `625` paid item IDs / `1,250` rows | `LIVE_VERIFIED`: 2026-05-14 sampled target rows are visible and sampled detail RPCs show effective `n:age_group` with no Missing age_group issue reproduced. Current Merchant prioritized page does not show Missing age group. `STALE_OR_SUPERSEDED`: downloaded CSV is still named `product_issues_2026-05-08_02-52-49.csv` and must not be treated as today's exact export | Do not request/perform repair from stale export alone; obtain current exact US/es export or authoritative all-row readback before reclassifying solved or repair-needed |
| Merchant Shopping capacity | `LIVE_VERIFIED` / `AUTOMATION_CAPABILITY_MISMATCH` | Not part of the prior command-layer seed | `LIVE_VERIFIED`: Merchant prioritized fixes page updated `3:09 AM May 14, 2026` shows `Over capacity for Shopping ads (outside of CSS program)` affecting `73.3K products (21%)`. Local diagnosis confirms Standard Shopping still served `17` impressions yesterday, so there is no proven total-serving outage, but the exact paid-cohort intersection remains unread because authenticated Merchant Chrome/account tooling is not usable in this automation runtime. | Keep the blocker active. Next exact unblock is an authenticated read-only Merchant product-level intersection check against the `780`-row `us_test_ready` / `paid_eligible` cohort; no product removals, source changes, or capacity request without owner decision |
| Non-US purchase proof | `STALE_OR_SUPERSEDED` | Order-level non-US purchase event currency/value proof remained unclosed in repo evidence; owner later said stop using tags/Event Quality/GA4 proof as launch-prep blocker, but do not overclaim measurement | `LIVE_READBACK_REQUIRED` for reporting claims | Reconcile with owner directive and fresh reporting evidence |
| Beach/Vacation URL | `REPO_KNOWN` | Stale Christmas SEO/social metadata on paid-candidate beach URL is locally mitigated by held Ads CSV excluding that handle; Shopify repair requires exact approval | `LIVE_READBACK_REQUIRED` before traffic | Keep URL excluded or request exact Shopify metadata repair approval |

## Live Readback Status

2026-05-14 read-only reconciliation confirmed, without writes:

- GB/CA/AU exact Search are enabled/eligible at the approved exact micro-cohort scope, with zero spend/click/impression/conversion data for yesterday.
- GB/CA/AU zero-impression action planning is locally complete and the fresh read-only monitor is complete: stale reporting filters were cleared, current keywords are enabled but `Eligible (Limited)` below first page bid, and a corrected blocked packet now rejects head/near-head terms and allows only long-tail validation candidates. No live keyword/ad/bid/status write occurred; active PDP landing sanitizer now passes public source readback, but candidate CPC validation is still required.
- A 105-row local keyword universe is now built and scored: `77` `GREEN`, `20` `YELLOW`, and `8` `RED` rows across US, GB, CA, and AU. Live promotion still requires route-level landing-clean proof, `$0.15` CPC validation, exact action-queue scope, reviewer pass, and after-state readback. After the 12:19 swim-route unblock, `36` GB/CA/AU `GREEN` rows are on clean routes and the exact authenticated validation CSV is ready.
- US Standard Shopping query/title diagnosis is ready and public PDP scope is narrowed: `41` US `GREEN` keyword rows and `29` US Shopping-ready rows were compared against visible Shopping query evidence and the `780`-variant paid cohort. Public route checks show `/collections/mommy-and-me`, `/collections/family-matching`, `/collections/pajamas`, `/collections/family-swimsuits`, and `/collections/daddy-and-me` clean; `/collections/vacation` is `404`, and `/collections/matching-dresses` still has `4` supplier hits. Public PDP preflight checked `10` unique candidate handles and found `8/10` source-clean; the authenticated export should start from `us_shopping_auth_export_public_clean_scope.csv` (`18` rows). The held-PDP packet plus local related-product filter now separates the rows: `3` rows excluded until product/vendor source-clean, `2` swim-trunks rows have a local theme seasonal-related fix ready but need approved live sync/readback, and `1` weak-fit source-clean row is allowed only if item-level impressions prove relevance. The export join prep packet loads `18` public-clean rows across `7` handles, `6` held rows across `3` handles, and provides `us_shopping_authenticated_item_export_template.csv` plus `run_us_shopping_auth_export_join_prep.py --export-csv` so the authenticated export can produce a joined decision CSV immediately.
- Standard Shopping is enabled/eligible, with `17` impressions, `0` clicks, `$0.00` cost, and `0.00` conversions/value for yesterday.
- Pinterest Ads Manager is not authenticated in the current controllable browser session.
- Merchant US/es age_group should be treated as sample-cleared but exact-export-pending, because the downloaded CSV is stale May 8 evidence.
- Merchant Shopping capacity local diagnosis is done: the warning is current and account-level, but paid-cohort impact is still unproven. Standard Shopping still served `17` impressions yesterday, which argues against a total serving outage, but a fresh authenticated Merchant intersection readback is still required.
- ROAS decision state from the saved readback does not justify scale, pause, negative, or bid/budget live action yet; bounded authority is active, and GB/CA/AU zero-impression state now requires same-day diagnosis and high-intent long-tail/auction-entry planning before any green-gated action.
- GB/CA/AU active Search landing public source has correct country/currency/shipping signals and now has `0` supplier/source-domain hits or URL-like brand attributes across two public header/cache variants. Candidate collection route work is unblocked for the current GB/CA/AU validation set: matching-dress, vacation, daddy, and swimwear rows have been rerouted to clean `mommy-and-me`, `family-matching`, or `family-swimsuits` routes.

Still needed:

- Current exact Merchant all-row export/readback for `US` / `es` / source `10627981690`, avoiding stale ready-download artifacts.
- Authenticated read-only Merchant capacity intersection for the current paid cohort and Standard Shopping serving; current automation runtime is missing the usable Chrome/account path for that exact readback.
- Authenticated Pinterest Ads Manager access and before-write readbacks for the paused US draft path.
- Validate the exact 36-row clean-route GB/CA/AU packet from `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-swim-route-unblock/gb_ca_au_36_clean_route_cpc_validation_rows.csv` against current active products, Keyword Planner/keyword UI auction entry at max `$0.15`, and anti-cannibalization ownership before any live Search packet. For US, use the universe first for Standard Shopping query/title/product-fit diagnosis and future Search/Pinterest packets.
- Next GB/CA/AU monitor and serving diagnosis now, not after impressions appear: check sales/ROAS, serving, eligibility, Quality Score gaps, auction entry/CPC cap, exact keyword competitiveness, saved long-tail alternatives, and CA/AU stale filter absence before any live action. Any candidate must prove it can enter at `$0.15` or stay blocked.
- Authenticated read-only Standard Shopping item-level export for campaign `23802638621`, joined first through `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-auth-export-join-prep/run_us_shopping_auth_export_join_prep.py --export-csv /path/to/authenticated-export.csv`, before any title/feed/product-group decision. Use `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-public-pdp-fit-preflight/us_shopping_auth_export_public_clean_scope.csv`, `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-held-pdp-repair-packet/US_SHOPPING_HELD_PDP_REPAIR_PACKET.md`, and `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-seasonal-related-filter/US_SHOPPING_SEASONAL_RELATED_FILTER_LOCAL_FIX.md` to keep held/excluded rows out of decisions unless repaired and re-read back clean, or unless the one weak-fit source-clean row is proven relevant by item-level export.
- Keep `/collections/swimsuits` excluded from live traffic until repaired, because it still has raw Shopify product JSON supplier vendor leaks. The current GB/CA/AU swimwear rows have been rerouted to the clean `/collections/family-swimsuits` route for CPC validation only.
- Full current Admin/Merchant/Pinterest active-product intersection before advertising all active products; the first prep map is saved, but upload/apply decisions still require exact approval and clean readbacks.

## Do Not Infer

- Do not infer live eligibility, spend, ROAS, or blockers from this file.
- Do not infer approval for out-of-bounds actions from bounded spend authority.
- Do not treat local review-only files as platform-ready.
- Do not create a second command layer under `AI_Team/`.
