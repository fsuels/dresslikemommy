# Marketing Assumption Log

Last updated: 2026-05-15 06:08 EDT

Use this file for important assumptions that affect paid-growth decisions, especially when evidence is repo-known, stale, sampled, or gated by approval/access.

| Date | Assumption | Evidence | Risk if wrong | How to verify or retire |
|---|---|---|---|---|
| 2026-05-14 | Bounded paid-media authority is active only inside the explicit caps and quality gates. | `ops/marketing/spend_authorization.md` status is `APPROVED_ACTIVE`; owner authorized spend within limits as long as goals are respected. | An agent could treat bounded authority as blanket approval for excluded surfaces or low-quality changes. | Re-check `spend_authorization.md`, `action_queue.md`, reviewer checklist, and before/after readbacks before any live paid action. |
| 2026-05-14 | Account access should be recovered from existing authenticated tabs, connectors, local secure credential sources, or current-session owner credentials before any blocker is declared. | Owner clarified account access should exist for Google Ads, Merchant Center, GA4/GTM, Search Console, Shopify Admin, Pinterest, GitHub, and business email; the previous Pinterest blocker was caused by probing a fresh unauthenticated page instead of the authenticated advertiser tab. `ops/ACCOUNT_ACCESS_PROTOCOL.md` now defines the recovery ladder. | Agents could waste time, open duplicate tabs, or mark a false P0 blocker while an authenticated tab/session already exists. | Before declaring access blocked, complete `ops/ACCOUNT_ACCESS_PROTOCOL.md`; record tab/session inventory, exact surface/account ID, prompt state, attempts, and next unblock label. Never persist credentials in repo files or evidence. |
| 2026-05-14 | Existing stopped-session supplier/source URL sanitizer changes are intentional local fixes. | Dirty diff touches public brand/vendor analytics surfaces and latest worklog anchor `2026-05-14-paid-landing-vendor-source-sanitizer-local` documents the local handoff. | Rewriting or reverting could reintroduce supplier URL leaks or lose a prepared fix. | Preserve changes; verify with diff checks and, before live sync, local/public source readbacks. |
| 2026-05-14 | Merchant US/es age_group old May 8 CSV should not drive repair decisions by itself. | Current marketing state says sampled 2026-05-14 detail readbacks did not reproduce Missing age_group, while the CSV download is stale May 8 evidence. | A needless Merchant or Shopify repair could mutate product/feed data. | Obtain current exact all-row readback/export for source `10627981690`, `US` / `es` / `United States`. |
| 2026-05-14 | Active-product advertising expansion needs a fresh public/Admin/Merchant/Pinterest intersection before uploads/applies. | Current state says the prep map is local/read-only and not approval to advertise all products. | Draft, inactive, supplier-leaking, stale, or unavailable products could enter paid traffic. | Build/read back current active, public, purchasable product scope and exclude unresolved products before any platform action. |
| 2026-05-14 | Father's Day or event-layer planning should prioritize Daddy-and-Me and father-inclusive family matching intent. | Current marketing state and decision log record the category strategy rule. | Seasonal ads may send shoppers to irrelevant or stale categories. | Review current collections/products and route event traffic only to intent-matched active products. |
| 2026-05-14 | 2026 expert strategy should favor high-intent, low-waste, non-cannibalizing traffic until data proves broader automation or scale. | `ops/marketing/expert_growth_playbook_2026.md` and official Google/Pinterest/OpenAI/Anthropic sources listed there. | Agents could buy cheap but low-intent traffic, duplicate query ownership, or broaden too early. | Daily search-term, Quality Score, ad, landing, product, purchase value, CPA, and ROAS readbacks decide expansion, negatives, bid changes, and scale. |
| 2026-05-14 | The growth system now treats zero impressions after 24 hours as an action trigger, not normal waiting. | Owner message: each day without growing sales is a failure; if after one day a campaign has 0 impressions, something needs to be done. | Agents could move too slowly and leave active campaigns with no learning. | Daily scorecard must show sales/ROAS and same-day diagnosis or long-tail/auction-entry action for zero-impression lanes. |
| 2026-05-14 | Active keyword campaigns need market/language intent maps; copied generic head terms are only starter controls. | Owner correction in current session; `GB_CA_AU_DAY1_ZERO_IMPRESSION_KEYWORD_STRATEGY_REPAIR.md`; updated `campaign_explorer.json`. | GB/CA/AU could keep aging with no impressions because keywords are too generic, too competitive, or mismatched to local vocabulary and buyer moments. | Fresh Google Ads readback, Keyword Planner/search-term/Quality Score evidence, landing sanitizer live readback, and reviewer pass before any live keyword/ad/bid/status action. |
| 2026-05-14 | Active Search keyword repair has a hard `$0.15` CPC ceiling; head terms and close-head variants are rejected unless they validate at that cap. | Owner correction in current session; fresh keyword UI showed below-first-page estimates around `$0.65-$0.74`; `CPC_015_LONG_TAIL_CORRECTION.md`; corrected blocked packet. | Agents could bid up or upload cosmetic local variants that still cannot run profitably at the `650% ROAS` target. | Validate any future candidate in Keyword Planner or keyword UI at max CPC `$0.15`; require no below-first-page warning or first-page estimate `<= $0.15`, clean landing, reviewer pass, and after-state plan. |
| 2026-05-14 | The keyword list should be large locally but small and validated when live. | `ops/marketing/keyword_factory_015_cpc_criteria.md`; owner asked why not get the list as big as possible. | Uploading too many unvalidated rows can create low-volume dead rows, noisy learning, self-cannibalization, or irrelevant phrase matches while the small budget cannot produce readable sales data. | Use the keyword factory score; promote only the top validated `10-20` exact rows per market first, then expand from search terms and planner evidence. |
| 2026-05-14 | US is the primary keyword-intelligence market. | Owner correction in current session; `ops/marketing/us_primary_keyword_lane.md`; campaign explorer has US Standard Shopping as the live US lane. | Operators could overfocus on GB/CA/AU Search repair and under-serve the biggest market. | Build US-first keyword universe; use it for Shopping query/title/product diagnostics, Pinterest creative language, and future US Search packets. |
| 2026-05-14 | Results/action mandate controls paid-growth behavior. | Owner correction in current session; durable docs updated across root/marketing/prompt/north-star files. | Agents could keep monitoring without fixing, improving, or preparing approval packets. | After every monitor/readback, require one of: fix now, execute approved bounded action, prepare exact approval packet, reroute safe lane, or hold with evidence that no action is valid. |
| 2026-05-14 | The 105-row keyword universe is a validation asset, not a platform upload file. | `ops/marketing/keyword_strategy.md`, `ops/marketing/keyword_scoring_rubric.md`, and `ops/marketing/keyword_universe.csv` created in this pass. | A future operator could upload too many unvalidated rows and create noisy low-volume learning or waste. | Before live use, validate active product fit, landing sanitizer, first-page estimate at max `$0.15`, anti-cannibalization owner, reviewer pass, and after-state readback plan. |
| 2026-05-14 | A strategy file that is not action-linked is a process defect. | Owner challenged side documents; `ops/scripts/audit_marketing_command_integration.py` now reports `0` risks after fixing the first `4`. | Future sessions could create plans with no follow-up and mistake documentation for progress. | Run `python3.13 ops/scripts/audit_marketing_command_integration.py --write-report --fail-on-risk`; any risk must be wired into AGENTS/action surfaces/continuity logs or marked generated/archive. |
| 2026-05-14 | Canonical continuity can drift even when the marketing audit passes. | `ops/AGENT_WORKLOG_utf8.md` existed as a tracked alternate worklog, and the canonical prompt still carried a stale hard-coded latest-anchor example in First actions. | A future agent could follow documented startup and still pick stale state from a side document or prompt literal. | Run `python3.13 ops/scripts/check_continuity_integrity.py --strict`; if it fails, repair canonical worklog/prompt/spend/cockpit wiring instead of creating another note. |
| 2026-05-14 | This automation runtime is not account-surface equivalent to a normal owner session. | Shell/repo writes/network/Playwright MCP are usable, but Chrome DevTools MCP is profile-locked, Computer Use interactive access is denied, and shell-side Playwright is not installed. | An agent could overclaim Merchant/Pinterest readback completeness or close a blocker without the authenticated intersection proof. | Treat account-dependent Merchant/Pinterest follow-ups as `AUTOMATION_CAPABILITY_MISMATCH` until a usable authenticated Chrome/account path is restored. |
| 2026-05-14 | Clean current PDP final URLs do not prove all future keyword collection routes are clean. | 11:18 public PDP readback passed, but 11:19 collection preflight found `matching-dresses` and `swimsuits` supplier JSON leaks, `vacation` `404`, and `daddy-and-me` Christmas pattern hits. | Live keyword rows could route paid traffic to supplier-leaking, broken, or seasonally mismatched pages. | Validate route cleanliness per candidate route before live use; keep affected `keyword_universe.csv` rows held until repaired, rerouted, or excluded. |
| 2026-05-14 | Exact 31-row GB/CA/AU CPC validation packet is a handoff artifact, not upload authority. | `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-cpc-validation-packet/GB_CA_AU_31_CLEAN_ROUTE_CPC_VALIDATION_PACKET.md`; no Google Ads API env keys and no `google.ads.googleads` package in this shell; authenticated GUI surfaces remain gated. | A future operator could mistake the packet for a Google Ads import list and bypass `$0.15` CPC validation. | Validate `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-cpc-validation-packet/gb_ca_au_31_clean_route_cpc_validation_rows.csv` in authenticated Google Ads/Keyword Planner; promote only passed rows through `action_queue.md` after reviewer pass and after-state plan. |
| 2026-05-14 | US Standard Shopping query/title candidates are hypotheses until item-level export proves them. | `US_STANDARD_SHOPPING_QUERY_TITLE_DIAGNOSIS.md` maps zero-click visible terms to paid-cohort candidates, but the current saved readback does not expose which item IDs/titles received those impressions. | A future operator could edit product/feed titles or product groups based on plausible mapping instead of proof. | Run authenticated read-only item-level export for campaign `23802638621`, then join it to `us_shopping_query_title_candidates.csv`; only proven mismatches become an approval packet. |
| 2026-05-14 | Public-clean US Shopping PDP rows are export candidates, not proof of item-level demand. | Public PDP preflight saved `18/24` candidate rows as `us_shopping_auth_export_public_clean_scope.csv`, with `5` held source/stale-copy rows and `1` title-fit review row. | A future operator could treat public PDP cleanliness as permission to edit titles/feed or ignore held rows without item-level impression proof. | Run authenticated item-level Standard Shopping export and join first to the public-clean scope; held rows require separate source/title-fit repair before they can influence title/feed decisions. |
| 2026-05-14 | Held US Shopping PDP rows are exclusion/repair gates, not hidden export candidates. | Held-PDP packet rechecked `6` held/review rows: `3` rows excluded until supplier/source-clean, `2` rows excluded until stale seasonal-copy-clean, and `1` source-clean weak-fit row allowed only if item-level impressions prove relevance. | A future operator could accidentally include supplier-leaking or stale seasonal PDPs in paid export/title repair decisions. | Use `US_SHOPPING_HELD_PDP_REPAIR_PACKET.md`; repair and public-readback excluded rows before paid use, or keep them excluded. |
| 2026-05-15 | The joined US Shopping item export is learning evidence, not title/feed/product-group authority. | The authenticated export joined `767` product rows and found `0` title/feed approval candidates; clicked-PDP public readback checked `27` clicked rows / `13` handles with `26/26` pass and `0` source-blocked clicked handles, but conversion value is still `$0.00`. | A future operator could edit product titles, feed attributes, product groups, bids, budgets, statuses, or negatives from click/impression evidence without proof that those edits will improve ROAS. | Continue read-only Merchant/feed eligibility and conversion/landing analysis. Convert only proven, narrow mismatches or clean product-expansion opportunities into exact approval packets before live writes. |
| 2026-05-14 | The swim-trunks live-sync packet is approval/readback prep, not theme-push authority. | `US_SHOPPING_SEASONAL_RELATED_FILTER_LIVE_SYNC_APPROVAL_PACKET.md` names exact approval language and pass criteria, but no live sync occurred in this automation run. | A future operator could push the theme without owner approval or treat the two swim-trunks rows as paid-clean before public source readback. | Get the exact approval phrase, push only `snippets/buy-box-similar-styles.liquid`, and require after-state public source readback with `0` stale seasonal hits before using those rows. |
| 2026-05-14 | The GB/CA/AU 36-row packet remains account-gated even though public routes refreshed clean. | Public refresh checked `24` fetches across `12` unique market/route URLs with `0` non-200s, `0` supplier/source-domain or URL-brand hits, and `0` stale seasonal/local-inventory trust hits; the shell still has no Google Ads env keys or `google.ads.googleads` package. | A future operator could mistake public landing proof for Keyword Planner/CPC proof and upload rows that cannot enter auctions at `$0.15`. | Run authenticated Google Ads/Keyword Planner validation at max `$0.15`; promote only pass rows through a fresh green action row with reviewer and after-state readback. |
| 2026-05-14 | The current GB/CA/AU CPC validation artifact should use canonical `/collections/matching-outfits` URLs, not redirecting `/collections/family-matching` URLs. | Canonical packet converted `11` rows and read back `24` public fetches across `12` unique market/route URLs with `0` redirects, `0` supplier/stale hits, and `0` non-200s. | A future operator could validate or upload a redirecting final URL set, adding friction or tracking/canonical ambiguity. | Use `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-36-row-cpc-canonical-url-packet/gb_ca_au_36_clean_route_cpc_validation_rows_canonical_urls.csv` for authenticated `$0.15` validation; retire this assumption after pass/fail rows are read back and a fresh action row is created. |
| 2026-05-14 | The CPC validation decision kit is a no-upload harness, not CPC proof. | `GB_CA_AU_CPC_VALIDATION_DECISION_KIT.md` generated local Keyword Planner inputs, a `72`-row exact+phrase validation matrix, a forecast export template, and parser, but no authenticated Ads forecast was available in this runtime. | A future operator could treat exact/phrase validation rows as live keyword additions without authenticated `$0.15` pass evidence. | In authenticated Ads/Keyword Planner, export forecast/readback rows, run `validate_keyword_planner_forecast_export.py`, and promote only `PASS_015_CPC_GATE` rows through a fresh `GREEN` action row after reviewer and after-state gates. |
| 2026-05-14 | The dirty collection cleanup packet is approval prep, not product-edit authority. | `COLLECTION_SOURCE_CLEANUP_APPROVAL_PACKET.md` found `3` source-vendor product rows across `/collections/swimsuits` and `/collections/matching-dresses`; the later US route unblock rerouted the affected local keyword rows away from those raw routes. | A future operator could edit Shopify product/vendor fields, upload keyword rows, or make feed/title decisions from the packet without approval and before/after readback. | Get the exact approval phrase in the packet before any Shopify product/vendor source cleanup; otherwise keep rows excluded or rerouted to clean routes. Retire only after public source readback shows `0` supplier hits across US/GB/CA/AU and the affected rows are reclassified. |
| 2026-05-14 | Pinterest Ads Manager access is restored only in the existing authenticated `549756244483` tab, not necessarily every newly opened Pinterest tab. | `P0_BLOCKER_FIX_READBACK.md` shows the selected existing Ads Manager tab is authenticated, while earlier fresh URL probes landed on public/login pages. | A future operator could open a fresh unauthenticated tab, see public Pinterest Ads, and falsely re-open the access blocker. | Use or claim the existing authenticated reporting tab first; read back advertiser `549756244483`, account/domain, Create menu, campaign count, serving count, spend, and prompts before any paused draft write. |
| 2026-05-14 | The CPC parser fix prevents a false policy block, but does not prove any GB/CA/AU row can enter auctions at `$0.15`. | `forecast_parser_smoke_summary.json` has one row each for pass/fail/low-volume/policy after the parser patch; the current controllable Google Ads page still redirects to sign-in. | A future operator could treat the smoke test as Keyword Planner proof. | Export real authenticated Keyword Planner/Ads forecast rows at max `$0.15`, run the patched parser, and promote only real `PASS_015_CPC_GATE` rows through a fresh `GREEN` action row. |
| 2026-05-14 | The US keyword route unblock removes local dirty-route friction, but it is not live Search authority. | `US_KEYWORD_ROUTE_UNBLOCK_PACKET.md` rerouted `23` US rows to clean replacement routes and public-read back the replacements with `200`, `0` supplier/url-brand hits, and `0` stale/trust hits. | A future operator could upload rerouted US rows without proving active-product fit, CPC/search feasibility, or anti-cannibalization ownership. | Build a small US validation packet only after active-product proof and authenticated `$0.15` CPC/search feasibility; require reviewer pass, fresh `GREEN` action row, and after-state readback before any live use. |
| 2026-05-14 | The Google Ads API CPC harness is a retry path, not CPC proof or upload authority. | `GOOGLE_ADS_API_CPC_FORECAST_RETRY_HARNESS.md`; compile and dry-run passed for `72` exact/phrase rows, but the non-dry run stopped before any API call because `GOOGLE_ADS_CUSTOMER_ID` is unset. | A future operator could mistake the harness or dry-run summary for authenticated Keyword Planner evidence and create live rows without real forecast proof. | In an account-capable shell, run `run_google_ads_api_cpc_forecast.py --customer-id 3990976848`, then parse `google_ads_api_cpc_forecast_rows.csv`; retire this assumption only after real `PASS_015_CPC_GATE` rows are read back and a fresh green action row is created. |
| 2026-05-15 | US public active-product proof is a validation source, not live Search authority. | `US_ACTIVE_PRODUCT_PROOF_PACKET.md` sampled `51` product pages across `5` clean US routes and found `47` public active-product pass rows plus `4` held/review rows. | A future operator could upload public-active rows without proving `$0.15` CPC/search feasibility, anti-cannibalization ownership, or live-action quality gates. | Build a small US validation packet only from `PUBLIC_ACTIVE_PRODUCT_PASS` rows, then run authenticated Google Ads/Keyword Planner or API forecast proof at max `$0.15`; retire only after pass rows are parsed and a fresh `GREEN` action row is created. |
| 2026-05-15 | The US Search active-product validation packet is a forecast input, not live Search authority. | `US_SEARCH_ACTIVE_PRODUCT_VALIDATION_PACKET.md` selected `12` public-active US `GREEN` rows and `24` exact/phrase forecast rows across `matching-outfits`, `mommy-and-me`, and `pajamas`; public route readback passed with `0` redirects, supplier hits, or stale/trust hits. | A future operator could upload the matrix or add keywords without authenticated `$0.15` pass evidence. | Run authenticated read-only Google Ads/Keyword Planner or API forecast proof at max `$0.15`, parse pass rows, reviewer-check them, and create a fresh `GREEN` action row before live use. |
| 2026-05-15 | Current aggregate Ads serving is a monitoring signal, not CPC or Shopping optimization proof. | Existing authenticated Google Ads UI readback showed enabled-campaign aggregate `6` impressions, `1` click, `$0.04` cost, and `0.00` conversions/value; visible GB exact Search stayed `0`; unfiltered broader search-term rows showed brand Search clicks/cost and visible Shopping rows at `0` clicks / `$0.00` cost. | A future operator could mistake the aggregate click or brand Search terms for GB/CA/AU long-tail validation or Shopping title/feed evidence. | Attribute the signal with campaign/item-level exports/readbacks. Retire only after authenticated CPC forecast rows or Shopping item-level export rows prove a specific action. |
| 2026-05-15 | The Pinterest paused-draft scope should use the refreshed `333`-variant public-clean file, not the older full `342` rows. | `PINTEREST_PAUSED_DRAFT_SCOPE_REFRESH.md` checked `32` public product pages and `32` image URLs from the prior `342` rows. `30/32` product pages and `32/32` images passed; `2` Mommy & Me PDPs exposed supplier/source-domain URLs, so `9` variants moved to public-source exclusions. | A future Pinterest operator could build paused draft product groups from supplier-leaking public PDPs, violating the paid-landing/source URL guardrail even if the Pinterest catalog rows previously looked clean. | Use `pinterest_paused_draft_refreshed_clean_scope.csv` for paused-draft prefill. Retire only if the `9` held variants are repaired through approved product/vendor cleanup and public source readback shows `0` supplier/source hits. |
| 2026-05-15 | The Pinterest 333 build-ready packet is exact approval prep, not current-session object authority. | `PINTEREST_333_PAUSED_DRAFT_BUILD_READY.md` names paused/draft object names and the exact current-session approval phrase, but no Pinterest UI Create/Save action occurred. | A future operator could mistake build-ready local object names for permission to create drafts without fresh approval. | Require the exact approval phrase before creating objects. Retire after before/after readbacks prove paused/draft status, `$0.00` spend, no serving, and no catalog/source/tag/CAPI/feed/audience mutation. |
| 2026-05-15 | The Google Shopping multilingual queue is an export/readback queue, not campaign/feed/title authority. | `GOOGLE_SHOPPING_MULTILINGUAL_EXPANSION_QUEUE.md` defines `US/en`, `US/es`, `CA/en`, `GB/en`, and `AU/en` read-only export steps. | A future operator could create Shopping campaigns or mutate feed/title/product groups from stale exports or native-language assumptions. | Run authenticated read-only exports and convert only proven clean rows into exact approval packets before any live write. |
| 2026-05-15 | The US/en Standard Shopping item export is now joined; clean clicked PDPs are conversion-analysis input, not edit authority. | Export join for campaign `23802638621` produced `767` rows, `65` clicks, `$14.17` cost, `$0.00` conversion value, and `0` title/feed repair candidates; clicked-PDP public readback passed `26/26` fetches with `0` source-blocked clicked handles. | A future operator could repeat the export lane or mutate product/feed/title/product groups from zero-conversion click evidence. | Do not repeat US/en export unless fresher data is needed. Use clicked PDPs for conversion/landing analysis and run Merchant `US/es` plus CA/GB/AU eligibility readbacks before Shopping expansion. |
| 2026-05-15 | Clicked Shopping PDP visible H1 ellipses are a shopper-message cleanup candidate, not feed/campaign authority. | `STANDARD_SHOPPING_CLICKED_TITLE_CONVERSION_APPROVAL_PACKET.md` checked `13` clicked PDPs covering `65` clicks / `$14.17` cost / `$0.00` conversion value. `12/13` visible H1s contain literal ellipses, covering `64` clicks / `$13.96` cost, while add-to-cart, customer-photo section markup, and hidden zero-review badge behavior remain present. | A future operator could either ignore a likely conversion-message issue on products getting paid clicks, or overreach by changing feed titles, campaigns, product groups, bids, budgets, statuses, or Shopify product data without approval. | Get the exact approval phrase before any Shopify title/display-title cleanup; verify before/after public H1/title/add-to-cart/price/source-clean/zero-review state. If approval is not granted, keep the packet as hold-with-evidence and continue Merchant/feed eligibility work. |
| 2026-05-15 | Pinterest paused-draft UI creation requires a valid daily budget before save. | Current-session approved UI attempt reached manual Catalog sales with campaign name `DLM_PIN_US_CATALOG_333_PAUSED_20260515` and status `Paused`, then Pinterest blocked save with `Enter a valid currency value to continue` and `Daily budgets must be $1.00 or more`. | A future operator could keep retrying the no-budget UI path or enter a budget value without explicit validation-only approval. | Retire only after owner approves entering the minimum `$1.00` daily budget while paused/unpublished/no-spend and after readback proves no serving/spend, or after a no-budget API/import route saves the draft. |
| 2026-05-15 | CA/GB/AU English `0` issue-export rows are now superseded by stronger all-products absence evidence, but still not campaign authority. | Current Merchant all-products export parsed `351,007` rows and found CA/en, GB/en, and AU/en each have `0` rows and no CAD/GBP/AUD feed labels or currencies. | A future operator could either build Shopping campaigns despite no feed rows, or overcorrect by mutating feeds/sources without approval. | Do not build CA/GB/AU Shopping. Retire only after authoritative Merchant feed/source proof shows target rows exist with expected country, language, currency, and approval state. |
| 2026-05-15 | The Merchant `US/es` no-write classification packet plus all-products export still are not repair authority. | Current classification packet found `53` paid-cohort issue items and only `3` unique paid-cohort attribute-repair candidates, while all `53` remain affected by over-capacity. Current all-products export proves `5,412` `US/es` rows / `772` paid-cohort rows, but lacks `source_id` and approval-status columns. | A future operator could edit Merchant feeds/products/source scope or request capacity from partial row-presence evidence. | Use this as repair/capacity packet input only. Retire after source/approval proof plus exact owner approval supports a narrow before/after repair or capacity action. |
| 2026-05-15 | The Merchant all-products TSV is row-presence evidence, not source-id or approval-status proof. | Downloaded TSV lacks `source_id` and approved/disapproved destination columns; `US/es` source `10627981690` remains tied by adjacent source/detail readback, not by TSV source-id. | A future operator could overclaim active approved count or exact source ownership and build/repair from incomplete evidence. | Use all-products export for market row presence and paid-cohort intersection only; require source/approval-status readback before any feed/source/campaign action. |
| 2026-05-15 | The browser RPC addendum confirms `US/es` source row presence but not repair/build authority. | Browser RPC all-products snapshot captured `351,007` sanitized rows; all `5,412` `US/es` rows are source `10627981690`, with `4,910` strict-approved raw product-list rows. | A future operator could incorrectly treat raw product-list status as clearing the current issue export and Shopping capacity blocker. | Use the RPC addendum to confirm source row presence/status only. Repair/capacity/Shopping decisions still need current issue/capacity gates, source/approval proof where relevant, reviewer pass, and exact owner approval. |
| 2026-05-15 | The `$1.00` Pinterest paused-draft approval did not clear the product-group scope blocker. | Validation-only follow-up searched `DLM_PIN_US_SHOPPING`, `mommy_me`, `family_matching`, `pajamas`, `Mommy`, and `Family Matching`; exact groups were absent and only broad groups were available (`Pajamas` `252`, Mommy & Me `445/1,011`, Family Matching `1,011+`, `All Products` `5,664`). | A future operator could treat the minimum-budget approval as permission to save a draft with broad product groups. | Do not save or launch until exact `201/103/29` groups are available or product-group creation/exposure is explicitly approved and read back. |
| 2026-05-15 | The Pinterest exact product-group packet is approval prep, not object authority. | `PINTEREST_EXACT_PRODUCT_GROUP_UNBLOCK_PACKET.md` derives exact group requirements from the `333` public-clean scope: Mommy & Me `201`, Family Matching `103`, Pajamas `29`, plus father-inclusive proof-only rows `43` variants / `4` products. | A future operator could create product groups, save a draft, or launch from this packet without approval or could over-split father-inclusive rows without exact platform proof. | Retire only after owner gives the exact approval phrase, the platform readback shows exact group counts, and final review confirms max `$5/day`, max `$0.15` CPC, and no out-of-scope catalog/source/feed/tag/CAPI/billing/Shopify mutation. |
| 2026-05-15 | Merchant capacity should be relieved by market/feed-country publishing scope, not product deletion. | `MERCHANT_PRIORITY_MARKET_CAPACITY_FIX_PACKET.md` classifies the `351,007` current Merchant rows and identifies `199,684` first-pass non-priority rows for removal from publishing scope while preserving USA English/Spanish and Europe-later groups. | A future operator could delete Shopify products, remove Europe too early, or mutate Google Ads product groups/campaigns instead of fixing the feed-country row explosion. | Retire only after exact platform preview/readback proves the non-priority groups were removed, total all-products rows dropped, USA English/Spanish remain present, and Canada English/French plus GB English can be exported with expected CAD/GBP rows. |
| 2026-05-15 | The Merchant capacity execution guard is preview/after-export acceptance criteria, not live write authority. | `MERCHANT_PRIORITY_MARKET_CAPACITY_EXECUTION_GUARD.md` and `merchant_capacity_platform_preview_acceptance.csv` define `41` exact preview rows, expected removal `199,684`, and protected USA English/Spanish counts. | A future operator could treat the guard as permission to save changes without a live platform preview, or skip the after-export validation. | Retire only after the live preview matches the acceptance CSV, the scoped cleanup is saved under the allowed boundary, and `build_merchant_capacity_execution_guard.py --after-export` passes on a fresh Merchant export. |
| 2026-05-15 | Shopify Markets `International` region pruning must be conservative and first-pass only. | `MERCHANT_SHOPIFY_MARKETS_REGION_PRUNE_PREVIEW.md` classifies `52/73` `International` regions as high-confidence owner-directed Asia/Africa/South America removal candidates and keeps `21` regions in preserve/hold-review, including duplicate `CA` and `AU`. | A future operator could disable the whole `International` market, remove Canada/Australia duplicate coverage blindly, or remove regions not explicitly covered by the owner direction. | Retire only after authenticated preview/readback proves the selected region changes preserve priority markets, reconcile to the feed-group guard, and a fresh Merchant export passes the after-export guard. |

## Add New Assumptions Like This

| Date | Assumption | Evidence | Risk if wrong | How to verify or retire |
|---|---|---|---|---|
| YYYY-MM-DD |  |  |  |  |
