# Marketing Blocker Board

Last reconciled: 2026-05-14 11:59 EDT
Detailed source of truth remains `ops/PROBLEM_TRACKER.md`.

| Priority | Blocker | Current compact status | Next unblock action |
|---|---|---|---|
| P0 | Pinterest Ads Manager controllable access | `LIVE_VERIFIED`: current controllable browser lands on public Pinterest Ads login/sign-up page; Create control not found; no Pinterest write occurred | Restore authenticated controllable Pinterest Ads Manager access for advertiser `549756244483`, then read back before any paused draft write |
| P0 | Fresh GB/CA/AU performance and search terms | `LIVE_VERIFIED_FILTERS_CLEARED_HEAD_TERMS_FAIL_31_CLEAN_ROWS_NEED_015_CPC_VALIDATION`: GB/CA/AU are enabled/eligible at approved scope with `0` cost/clicks/impressions/conversions/value in the displayed monitor window. Stale `Keyword: "human hair wigs"` filters were removed from all three search-term pages; no search terms are available. Keyword/RSA/final URL checks passed, but active keyword rows are `Eligible (Limited)` below first page bid around `$0.65-$0.74`, which fails the hard `$0.15` CPC cap. Active PDP final URL source now passes the sanitizer readback; local reroute produced `31` GB/CA/AU `GREEN` rows on clean routes for validation, not upload. | Do not execute keyword/bid/status/negative action until the exact 31-row packet is validated at `$0.15`; no head-term bid-up and no close-head variants |
| P1 | Standing bounded spend authority | `APPROVED_ACTIVE` inside explicit paid-media caps and quality gates only | Do not treat as blanket approval; live writes still need exact green row, fresh readback, reviewer pass, after-state readback, and excluded-surface checks |
| P1 | Merchant US/es age_group | `LIVE_VERIFIED_SAMPLE_CLEAR__STALE_EXPORT_SUPERSEDED__AUTOMATION_CAPABILITY_MISMATCH`: samples no longer reproduce Missing age_group; old May 8 CSV still shows the old `625` IDs but is stale/superseded for current action, and this automation runtime lacks a usable authenticated Merchant Chrome/account path for proving a fresh all-row export | Obtain current exact US/es export/readback in an authenticated Merchant session before deciding solved vs repair-needed; do not request repair approval from stale export alone |
| P1 | Merchant Shopping Ads capacity | `LIVE_VERIFIED_ACCOUNT_WARNING__LOCAL_DIAGNOSIS_DONE__AUTOMATION_CAPABILITY_MISMATCH`: prioritized fixes page updated `3:09 AM May 14, 2026` shows `Over capacity for Shopping ads (outside of CSS program)` / `73.3K products (21%)`; local diagnosis also shows Standard Shopping still served `17` impressions yesterday, so paid-cohort impact is still unproven | Run an authenticated read-only Merchant product-level intersection against the active `780`-row `us_test_ready` / `paid_eligible` cohort; no product removals/source changes/capacity request without owner decision |
| P1 | Active paid Search landing supplier/source URL leak | `LIVE_VERIFIED_SOURCE_CLEAN_FOR_CURRENT_PDP`: 2026-05-14 11:18 public GB/CA/AU active PDP final URL source has `0` supplier/source-domain hits and `0` URL-like brand attributes across two header/cache variants. This clears the current final URL sanitizer blocker, not every future collection route. | Proceed only to authenticated `$0.15` CPC validation for rows using clean routes; do not upload or add keywords yet |
| P1 | Candidate collection landing cleanliness | `PARTIAL_REROUTE_DONE__SWIM_ROUTE_STILL_HELD`: public preflight found `/collections/mommy-and-me`, `/collections/family-matching`, and `/collections/pajamas` clean. Local reroute moved GB/CA/AU matching-dress, vacation, and daddy rows to clean product-relevant routes, and this run prepared the exact 31-row authenticated CPC validation packet (`GB=11`, `CA=10`, `AU=10`). `/collections/swimsuits` still leaks raw Shopify product JSON supplier vendors and `5` swimwear rows remain held. | Validate `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-cpc-validation-packet/gb_ca_au_31_clean_route_cpc_validation_rows.csv` in authenticated Ads/Keyword Planner; repair/reroute/exclude swimwear before live traffic, with fresh approval if Shopify product/vendor metadata must change |
| P1 | ES/IT native review | Golden Daisy microtest is review-only; `platform_use_ready=false` | Get native review signoff, rerun validator, then request exact platform-use approval |
| P1 | RO/PT/GR/FR/BE Google Search build continuation | RO absent/blocked, PT/GR absent, FR stale, BE throttle; completed countries must not be re-uploaded | One-country-at-a-time preview/readback after cooldown and no-duplicate checks |
| P2 | Beach/Vacation stale SEO/social metadata | Paid traffic to stale URL is locally held/excluded | Keep held CSV path or request exact Shopify SEO/social repair approval |

## Board Rules

- Every blocker here must map to a `PROB-*` entry or a named worklog anchor.
- Do not close a blocker here unless the detailed tracker is solved, disproven, superseded, or explicitly gated.
- A blocker in one lane is a routing signal; keep independent lanes moving.
