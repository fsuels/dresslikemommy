# Marketing Blocker Board

Last reconciled: 2026-05-14 16:05 EDT
Detailed source of truth remains `ops/PROBLEM_TRACKER.md`.

| Priority | Blocker | Current compact status | Next unblock action |
|---|---|---|---|
| P0 | False account-access blockers | `PROTOCOL_INSTALLED_NO_CREDENTIAL_PERSISTENCE`: `ops/ACCOUNT_ACCESS_PROTOCOL.md` now requires agents to check existing authenticated tabs/sessions, connectors/local secure credential sources, direct authenticated account navigation, and current-session credentials before declaring Google Ads, Merchant Center, GA4/GTM, Search Console, Shopify Admin, Pinterest, GitHub, or business email blocked. Owner credentials are current-session only and must not be saved or written to repo/evidence. | Do not open a generic P0 access blocker from a fresh login page alone. Use `ACCESS_RECOVERY_REQUIRED`, `MFA_OR_CAPTCHA_REQUIRED`, `PERMISSION_REQUIRED`, or `ACCOUNT_SWITCH_REQUIRED` after the ladder is attempted, and keep unrelated safe lanes moving |
| P0 | Pinterest Ads Manager controllable access | `ACCESS_RESTORED_DRAFT_CHECK_DONE_NO_WRITE`: the authenticated existing Ads Manager tab is controllable for advertiser `549756244483`; account/domain read back as `Dress Like Mommy | Matching Family Outfits` / `dresslikemommy.com`, Create menu opens with `Create campaign` and `Load existing campaign draft`, reporting shows `0 campaigns`, `0 currently being served`, `$0.00` spend, and `0` impressions for `05/07/2026 - 05/14/2026`. 16:05 existing-draft sheet readback says there are no saved campaign drafts. No Pinterest write occurred. | Access blocker is fixed. Next Pinterest action is `Create new campaign` only through the approved paused US draft spec; stop before budget/bid/enablement/launch/publish/audience/source/feed/tag/CAPI changes unless the spec and approval gates are satisfied |
| P0 | Fresh GB/CA/AU performance and search terms | `LIVE_VERIFIED_FILTERS_CLEARED_HEAD_TERMS_FAIL_DECISION_KIT_READY_AUTH_CPC_REQUIRED_PARSER_FIXED`: GB/CA/AU are enabled/eligible at approved scope with `0` cost/clicks/impressions/value in the displayed monitor window. Stale `Keyword: "human hair wigs"` filters were removed from all three search-term pages; no search terms are available. Keyword/RSA/final URL checks passed, but active keyword rows are `Eligible (Limited)` below first page bid around `$0.65-$0.74`, which fails the hard `$0.15` CPC cap. Active PDP final URL source now passes the sanitizer readback; local reroute produced `36` GB/CA/AU `GREEN` rows on clean routes, the current canonical validation CSV has `0` redirecting final URLs, and the decision kit provides `72` exact+phrase validation rows plus a patched forecast parser that no longer treats ordinary `Eligible (Limited)` as a policy block. | Do not execute keyword/bid/status/negative action until the exact canonical 36-row packet is validated at `$0.15` in authenticated Keyword Planner/Ads and parsed into pass rows; no head-term bid-up and no close-head variants |
| P1 | Standing bounded spend authority | `APPROVED_ACTIVE` inside explicit paid-media caps and quality gates only | Do not treat as blanket approval; live writes still need exact green row, fresh readback, reviewer pass, after-state readback, and excluded-surface checks |
| P1 | Merchant US/es age_group | `LIVE_VERIFIED_SAMPLE_CLEAR__STALE_EXPORT_SUPERSEDED__AUTOMATION_CAPABILITY_MISMATCH`: samples no longer reproduce Missing age_group; old May 8 CSV still shows the old `625` IDs but is stale/superseded for current action, and this automation runtime lacks a usable authenticated Merchant Chrome/account path for proving a fresh all-row export | Obtain current exact US/es export/readback in an authenticated Merchant session before deciding solved vs repair-needed; do not request repair approval from stale export alone |
| P1 | Merchant Shopping Ads capacity | `LIVE_VERIFIED_ACCOUNT_WARNING__LOCAL_DIAGNOSIS_DONE__AUTOMATION_CAPABILITY_MISMATCH`: prioritized fixes page updated `3:09 AM May 14, 2026` shows `Over capacity for Shopping ads (outside of CSS program)` / `73.3K products (21%)`; local diagnosis also shows Standard Shopping still served `17` impressions yesterday, so paid-cohort impact is still unproven | Run an authenticated read-only Merchant product-level intersection against the active `780`-row `us_test_ready` / `paid_eligible` cohort; no product removals/source changes/capacity request without owner decision |
| P1 | US Standard Shopping query/title proof | `AUTH_EXPORT_JOIN_PREP_READY__AUTH_EXPORT_REQUIRED`: current Standard Shopping has `17` impressions, `0` clicks/cost/conversions; visible yesterday terms are relevant enough that no negative is justified. Local packet maps those terms to paid-cohort candidate handles. Public PDP preflight produced `18` public-clean rows for authenticated export. The held-PDP packet keeps `3` rows excluded until supplier/source-clean, and the two swim-trunks seasonal rows now have a local related-product theme fix plus standalone scoped live-sync approval/readback packet but still need exact approval, live sync, and public readback before paid use. This run added the local join script/template so the future authenticated export can immediately classify public-clean, held, and unmatched rows. The `1` source-clean weak-fit row is allowed only if item-level impressions prove relevance | Run authenticated read-only product-item export for campaign `23802638621`, then run `python3.13 dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-us-shopping-auth-export-join-prep/run_us_shopping_auth_export_join_prep.py --export-csv /path/to/authenticated-export.csv`; keep held/excluded rows out of decisions unless repaired/read back clean or proven relevant. If owner approves the swim-trunks theme fix, use `US_SHOPPING_SEASONAL_RELATED_FILTER_LIVE_SYNC_APPROVAL_PACKET.md` and push only the snippet with before/after public source readback. |
| P1 | Active paid Search landing supplier/source URL leak | `LIVE_VERIFIED_SOURCE_CLEAN_FOR_CURRENT_PDP`: 2026-05-14 11:18 public GB/CA/AU active PDP final URL source has `0` supplier/source-domain hits and `0` URL-like brand attributes across two header/cache variants. This clears the current final URL sanitizer blocker, not every future collection route. | Proceed only to authenticated `$0.15` CPC validation for rows using clean routes; do not upload or add keywords yet |
| P1 | Candidate collection landing cleanliness | `CPC_DECISION_KIT_READY_US_ROUTE_REROUTE_DONE_DIRTY_ROUTE_PACKET_READY_AUTH_VALIDATION_PENDING`: public preflight found `/collections/mommy-and-me`, `/collections/family-matching`, `/collections/pajamas`, and `/collections/family-swimsuits` clean. Local reroute moved GB/CA/AU matching-dress, vacation, daddy, and swimwear rows to clean product-relevant routes, and the exact 36-row authenticated CPC validation scope remains `GB=12`, `CA=12`, `AU=12`. 14:57 EDT canonical public readback converted `11` `/collections/family-matching` rows to `/collections/matching-outfits` and passed `24/24` fetches across `12` market/route URLs with `0` redirects, `0` supplier/url-brand hits, and `0` stale seasonal/local-inventory trust hits. 15:19 EDT decision kit generated market-specific Keyword Planner inputs, a `72`-row exact+phrase matrix, and a forecast parser. 15:38 EDT dirty-route packet shows `/collections/swimsuits` still has `2` source-vendor products and `/collections/matching-dresses` has `1`. 15:59 EDT US route unblock rerouted all `23` US rows from `/collections/vacation`, `/collections/matching-dresses`, `/collections/swimsuits`, and `/collections/daddy-and-me` to clean product-relevant routes; replacement-route public readback passed. | Validate `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-36-row-cpc-canonical-url-packet/gb_ca_au_36_clean_route_cpc_validation_rows_canonical_urls.csv` in authenticated Ads/Keyword Planner, then run the decision-kit parser before any live traffic. For future US Search, build a validation packet only after active-product proof and `$0.15` CPC/search feasibility. Keep original dirty routes excluded unless owner-approved Shopify product/vendor source cleanup passes public readback |
| P1 | ES/IT native review | Golden Daisy microtest is review-only; `platform_use_ready=false` | Get native review signoff, rerun validator, then request exact platform-use approval |
| P1 | RO/PT/GR/FR/BE Google Search build continuation | RO absent/blocked, PT/GR absent, FR stale, BE throttle; completed countries must not be re-uploaded | One-country-at-a-time preview/readback after cooldown and no-duplicate checks |
| P2 | Beach/Vacation stale SEO/social metadata | Paid traffic to stale URL is locally held/excluded | Keep held CSV path or request exact Shopify SEO/social repair approval |

## Board Rules

- Every blocker here must map to a `PROB-*` entry or a named worklog anchor.
- Do not close a blocker here unless the detailed tracker is solved, disproven, superseded, or explicitly gated.
- A blocker in one lane is a routing signal; keep independent lanes moving.
