# Marketing Blocker Board

Last reconciled: 2026-05-15 07:17 EDT
Detailed source of truth remains `ops/PROBLEM_TRACKER.md`.

| Priority | Blocker | Current compact status | Next unblock action |
|---|---|---|---|
| P0 | Authenticated GB/CA/AU CPC validation | `BASIC_ACCESS_REQUEST_SUBMITTED__PENDING_GOOGLE_REVIEW__NO_PASS_NO_GREEN`: existing authenticated Google Ads access was found and Keyword Planner accepted the `72` exact/phrase inputs, but the exported plan stayed invalid (`United States`, `All languages`, `Broad`, `Maximize conversions`) and parser returned `0` pass rows. On 2026-05-15 the secure local API config checks `ready: true`; owner screenshot readback shows `Dress Like Mommy Manager 700-107-9966` linked under client account `399-097-6848`. The read-only Google Ads API forecast harness reaches Google Ads API but is blocked by Explorer access. Basic Access application was submitted at 03:13 EDT for manager `700-107-9966`, permissible use `Researching keywords and recommendations`; Google confirmation says the API Compliance team received the ticket and typical review is within three business days. `info@dresslikemommy.com` is connected through Outlook and searched clean again as of 06:06 EDT. Existing authenticated Google Ads UI readback showed enabled-campaign aggregate current serving (`6` impressions, `1` click, `$0.04`, `0.00` conversions/value), but visible GB exact Search stayed at `0` impressions/clicks/cost and the unfiltered broader search-term view showed historical brand Search clicks/cost rather than GB/CA/AU packet pass evidence. `testhqfinds@gmail.com` remains unconnected because the Gmail connector OAuth flow is stuck. No live Ads campaign/feed/spend write occurred and no pass rows exist. | Watch for Google API Compliance / Google Ads API Basic Access mail in `info@dresslikemommy.com`; retry `testhqfinds@gmail.com` by Gmail connector or logged-in Gmail UI later. After approval, rerun the API harness and parser. Do not resubmit unless Google asks for changes. Do not upload/add/bid/status/negative action before real `PASS_015_CPC_GATE` rows exist |
| P1 | Pinterest catalog sales launch/saved-draft scope | `IMPORTED_FILTERS_READBACK_201_103_29_PRODUCTS_ZERO_NO_LAUNCH`: the authenticated Pinterest UI remains controllable for advertiser `549756244483`. Owner approved the exact product-group packet phrase and then approved an upload-capable exact CSV import path. The exact item-ID CSV imported and created `DLM_PIN_US_SHOPPING_MOMMY_ME_333`, `DLM_PIN_US_SHOPPING_FAMILY_MATCHING_333`, and `DLM_PIN_US_SHOPPING_PAJAMAS_333`. Edit readback confirms exact item-ID filter payload counts `201/103/29`, but product detail pages still show `0` selected/products, empty previews, `Promote` disabled, and `This product group updates every 24 hours`. No campaign/draft launch occurred, no broad group was selected, and no source/feed/tag/CAPI/billing/Shopify mutation occurred. | Freshly read back the exact groups after Pinterest resolves the import. Proceed to final launch review only if usable product counts match exact active-clean scope, max `$5/day`, max `$0.15` CPC, exact groups, and no source/feed/tag/CAPI/billing/Shopify changes are confirmed. Do not launch or save broad groups |
| P2 | Account-access false-blocker prevention | `RESOLVED_GUARDRAIL_INSTALLED_NO_CREDENTIAL_PERSISTENCE`: `ops/ACCOUNT_ACCESS_PROTOCOL.md` now requires agents to check existing authenticated tabs/sessions, connectors/local secure credential sources, direct authenticated account navigation, and current-session credentials before declaring Google Ads, Merchant Center, GA4/GTM, Search Console, Shopify Admin, Pinterest, GitHub, or business email blocked. Owner credentials are current-session only and must not be saved or written to repo/evidence. | Do not open a generic P0 access blocker from a fresh login page alone. Use `ACCESS_RECOVERY_REQUIRED`, `MFA_OR_CAPTCHA_REQUIRED`, `PERMISSION_REQUIRED`, or `ACCOUNT_SWITCH_REQUIRED` after the ladder is attempted, and keep unrelated safe lanes moving |
| P1 | Standing bounded spend authority | `APPROVED_ACTIVE` inside explicit paid-media caps and quality gates only | Do not treat as blanket approval; live writes still need exact green row, fresh readback, reviewer pass, after-state readback, and excluded-surface checks |
| P1 | Merchant US/es age_group | `ALL_PRODUCTS_EXPORT_CAPTURED__US_ES_BLOCKED_REPAIR_APPROVAL_GATED`: current Merchant all-products export parsed `351,007` rows. `US/es` has `5,412` all-product rows / `5,301` in-stock rows / `772` TSV paid-cohort rows, while current issue evidence still has `1,453` issue rows / `53` paid-cohort issue items and `432` Missing age group rows. Browser RPC source/status addendum confirms all `5,412` `US/es` rows are source `10627981690` and `4,910` are strict-approved by raw product-list status. The no-write classification packet narrowed paid-cohort attribute-repair exposure to `3` unique paid-cohort items, but all `53` paid-cohort issue items remain affected by Shopping ads over-capacity. | Do not upload/feed-edit/title-edit/product-scope/capacity-request/campaign-build. Next valid step is a narrow owner-approved repair/capacity/source decision only after source/approval proof is sufficient |
| P1 | Merchant Shopping Ads capacity | `SHOPIFY_REGION_PRUNE_DONE__MERCHANT_SYNC_PENDING__SHOPPING_BLOCKED`: current-session exact Shopify Markets cleanup removed only the `52` approved non-priority `International` regions; `International` went from `73` to `21`, separate US/Canada/UK/EU/Australia markets stayed active, and duplicate `CA`/`AU` stayed preserved. Fresh Merchant browser-RPC re-export immediately afterward still captured `351,007` rows, the after-export guard failed with `199,684` remaining first-pass removal rows, and target rows remain USA English `5,491`, USA Spanish `5,412`, CA/en `0`, CA/fr `0`, GB/en `0`. The Shopify fix is real, but Merchant/Google product generation has not propagated or still needs Google & YouTube/Merchant publishing sync/control action. | Do not build Shopping and do not repeat the same Shopify region prune. Next unblock is Google & YouTube/Merchant publishing sync/control readback or delayed re-export until `build_merchant_capacity_execution_guard.py --after-export` passes and Canada English/French plus GB English rows exist. No campaign/feed/product-group/bid/budget/status/conversion action is valid before that |
| P1 | US Standard Shopping query/title proof | `CLICKED_TITLE_CLEANUP_PACKET_READY__OWNER_APPROVAL_REQUIRED`: read-only products export for campaign `23802638621` joined `767` paid-cohort rows for `Apr 18-May 14, 2026`: `112` rows with impressions, `65` clicks, `$14.17` cost, `$0.00` conversion value. The join produced `85` public-clean matches, `30` held matches, `652` unmatched rows, and `0` feed-title repair candidates. A follow-up public readback checked `27` clicked export rows across `13` unique handles / `26` fetches; `26/26` passed and `0` clicked handles were source-blocked. The conversion-title packet found `12/13` clicked PDP visible H1s have literal ellipses, covering `64` clicks / `$13.96` cost with `$0.00` conversion value. No negative, bid, budget, status, feed-title, product-group, product-scope, or theme write is justified from this packet alone | Exact owner approval can unlock a no-feed/no-campaign Shopify title/display-title cleanup only for the listed clicked PDPs, with before/after public H1/title/add-to-cart/price/source-clean/zero-review readback. Continue Shopping with Merchant/feed eligibility in parallel; if owner approves the swim-trunks theme fix, use `US_SHOPPING_SEASONAL_RELATED_FILTER_LIVE_SYNC_APPROVAL_PACKET.md`; otherwise keep held rows excluded. |
| P1 | Google Shopping multilingual expansion proof | `PRIORITY_MARKET_ORDER_UPDATED__CA_FR_GB_ABSENT`: owner requested Shopping and other-language traffic, now prioritized as USA English/Spanish, Canada English/French, GB English, then Europe. `US/en` Standard Shopping item export and clicked-PDP public readback are complete. `US/es` has all-product rows but remains blocked by current issue/export and over-capacity evidence. CA/en, CA/fr, and GB/en have `0` current all-products rows and no CAD/GBP feed-label proof. | Do not create Shopping campaigns, change feed/title/product groups, product scope, budget, bid, status, capacity, or conversion settings. Next unblock is priority-market capacity cleanup, then authoritative Merchant proof rows exist for Canada English/French and GB English before any Shopping build |
| P1 | Active paid Search landing supplier/source URL leak | `LIVE_VERIFIED_SOURCE_CLEAN_FOR_CURRENT_PDP`: 2026-05-14 11:18 public GB/CA/AU active PDP final URL source has `0` supplier/source-domain hits and `0` URL-like brand attributes across two header/cache variants. This clears the current final URL sanitizer blocker, not every future collection route. | Proceed only to authenticated `$0.15` CPC validation for rows using clean routes; do not upload or add keywords yet |
| P1 | Candidate collection landing cleanliness | `API_CLIENT_READY_CONFIG_MISSING_US_ACTIVE_PRODUCT_VALIDATION_PACKET_READY_DIRTY_ROUTE_PACKET_READY_AUTH_VALIDATION_PENDING`: public preflight found `/collections/mommy-and-me`, `/collections/family-matching`, `/collections/pajamas`, and `/collections/family-swimsuits` clean. Local reroute moved GB/CA/AU matching-dress, vacation, daddy, and swimwear rows to clean product-relevant routes, and the exact 36-row authenticated CPC validation scope remains `GB=12`, `CA=12`, `AU=12`. 14:57 EDT canonical public readback passed `24/24` fetches across `12` market/route URLs. 15:59 EDT US route unblock rerouted all `23` US rows from dirty/broken routes to clean product-relevant routes; replacement-route public readback passed. 04:06 EDT public active-product proof sampled `51` US product pages across `5` clean routes: `47` passed as public active-product candidates and `4` stayed held/review. 04:27 EDT no-upload US Search packet selected `12` public-active `GREEN` rows and `24` exact/phrase forecast rows across `matching-outfits`, `mommy-and-me`, and `pajamas`; `6/6` public route fetches passed with `0` redirects, supplier hits, or stale/trust hits. Dirty raw routes still exist as separate cleanup/exclusion surfaces. | Validate `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-14-automation-36-row-cpc-canonical-url-packet/gb_ca_au_36_clean_route_cpc_validation_rows_canonical_urls.csv` with the API harness after Basic Access is approved, or authenticated Ads/Keyword Planner with explicit market/match/max-CPC readback, then run the decision-kit parser before any live traffic. For future US Search, validate `dresslikemommy-growth-2026/02_AUDIT_PACKETS/2026-05-15-automation-us-search-active-product-validation-packet/us_search_12_active_product_cpc_validation_matrix.csv` only after authenticated `$0.15` CPC/search feasibility is available; no upload/add/bid/status action until pass rows and a fresh green row exist. Keep original dirty routes excluded unless owner-approved Shopify product/vendor source cleanup passes public readback |
| P1 | ES/IT native review | Golden Daisy microtest is review-only; `platform_use_ready=false` | Get native review signoff, rerun validator, then request exact platform-use approval |
| P1 | RO/PT/GR/FR/BE Google Search build continuation | RO absent/blocked, PT/GR absent, FR stale, BE throttle; completed countries must not be re-uploaded | One-country-at-a-time preview/readback after cooldown and no-duplicate checks |
| P2 | Beach/Vacation stale SEO/social metadata | Paid traffic to stale URL is locally held/excluded | Keep held CSV path or request exact Shopify SEO/social repair approval |

## Board Rules

- Every blocker here must map to a `PROB-*` entry or a named worklog anchor.
- Do not close a blocker here unless the detailed tracker is solved, disproven, superseded, or explicitly gated.
- A blocker in one lane is a routing signal; keep independent lanes moving.
